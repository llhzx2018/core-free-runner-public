# VF Server Ops example configuration
# NEVER place real credentials in Git.

LOCAL_RETENTION=3
GOOGLE_BACKUP_POOL_ENABLED=false
BACKBLAZE_B2_ENABLED=false
REMOTE_ENCRYPTION_ENABLED=true

# rclone remote names only; credentials live in runtime rclone config.
GOOGLE_REMOTE_1=gdrive1
GOOGLE_REMOTE_2=gdrive2
GOOGLE_REMOTE_3=gdrive3
B2_REMOTE=b2
