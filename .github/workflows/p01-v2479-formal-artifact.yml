name: P01 V2.47.9 Formal Artifact and Publish

on:
  pull_request:
    paths:
      - '.github/workflows/p01-v2479-formal-artifact.yml'

permissions:
  contents: read

concurrency:
  group: p01-v2479-formal-artifact
  cancel-in-progress: false

jobs:
  artifact:
    if: github.head_ref == 'temp/p01-v2479-formal-artifact-20260911'
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    env:
      PRODUCT_REPO: llhzx2018/vf-start
      FORMAL_SHA: 89df52cede9f03c88627396e66afe97325214353
      CANDIDATE_BRANCH: release/v2.47.9-candidate-20260911
      SOURCE_TAG: v2.47.8
      SOURCE_SHA: c1806005c0f89d9794844124264cb15a7ffcb1fc
      SOURCE_VERSION: 2.47.8
      TARGET_VERSION: 2.47.9
      TARGET_TAG: v2.47.9
      EXPECTED_SCHEMA: '2026090401'
      CANDIDATE_RUN: '34662954221'
      PRIVATE_READ_TOKEN: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
      RELEASE_TOKEN: ${{ secrets.VF_RELEASE_WRITE_TOKEN }}

    steps:
      - name: Exact formal authority
        shell: bash
        run: |
          set -Eeuo pipefail
          test -n "$PRIVATE_READ_TOKEN" && test -n "$RELEASE_TOKEN"
          test "$(GH_TOKEN="$PRIVATE_READ_TOKEN" gh api "repos/${PRODUCT_REPO}/git/ref/heads/main" --jq '.object.sha')" = "$FORMAL_SHA"
          test "$(GH_TOKEN="$PRIVATE_READ_TOKEN" gh api "repos/${PRODUCT_REPO}/git/ref/heads/${CANDIDATE_BRANCH}" --jq '.object.sha')" = "$FORMAL_SHA"
          test "$(GH_TOKEN="$PRIVATE_READ_TOKEN" gh api "repos/${PRODUCT_REPO}/git/ref/tags/${SOURCE_TAG}" --jq '.object.sha')" = "$SOURCE_SHA"
          if GH_TOKEN="$PRIVATE_READ_TOKEN" gh api "repos/${PRODUCT_REPO}/git/ref/tags/${TARGET_TAG}" >/dev/null 2>&1; then echo BLOCK=TARGET_TAG_ALREADY_EXISTS; exit 81; fi
          test "$(GH_TOKEN="$RELEASE_TOKEN" gh api "repos/${PRODUCT_REPO}" --jq '.permissions.push')" = true
          echo P01_V2479_FORMAL_AUTHORITY=PASS

      - name: Checkout exact formal source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: llhzx2018/vf-start
          ref: ${{ env.FORMAL_SHA }}
          token: ${{ env.PRIVATE_READ_TOKEN }}
          persist-credentials: false
          fetch-depth: 0
          fetch-tags: true
          path: vf-start

      - name: Checkout exact V2.47.8 baseline
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: llhzx2018/vf-start
          ref: ${{ env.SOURCE_SHA }}
          token: ${{ env.PRIVATE_READ_TOKEN }}
          persist-credentials: false
          fetch-depth: 1
          path: baseline

      - name: Checkout proven artifact helper
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: llhzx2018/vf-start
          ref: 1dd635d8e8d4485736458672fb7a5be397d94f89
          token: ${{ env.PRIVATE_READ_TOKEN }}
          persist-credentials: false
          fetch-depth: 1
          path: helper

      - name: Checkout proven artifact template
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: llhzx2018/vf-start
          ref: 0439b0df027ae0bc6c21a8dbf91b066a564130f9
          token: ${{ env.PRIVATE_READ_TOKEN }}
          persist-credentials: false
          fetch-depth: 1
          path: template

      - name: Checkout proven Atomic primitives
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: llhzx2018/core-free-runner-public
          ref: 0468b28ae57c368f80b03f85060318667bcc5834
          persist-credentials: false
          fetch-depth: 1
          path: proven

      - name: Setup PHP 8.4
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240
        with:
          php-version: '8.4'
          extensions: pdo_sqlite,sqlite3,curl,zip,simplexml,mbstring,openssl,fileinfo,gd
          coverage: none

      - name: Rebuild deterministic FULL and Atomic UPDATE
        shell: bash
        run: |
          set -Eeuo pipefail
          test "$(git -C vf-start rev-parse HEAD)" = "$FORMAL_SHA"
          test "$(git -C baseline rev-parse HEAD)" = "$SOURCE_SHA"
          test "$(tr -d '\r\n ' < vf-start/VERSION)" = "$TARGET_VERSION"
          test "$(tr -d '\r\n ' < vf-start/src/VERSION.txt)" = "$TARGET_VERSION"
          grep -Fq "define('VF_VERSION', '${TARGET_VERSION}');" vf-start/src/app/bootstrap.php
          test "$(php -r '$p=json_decode(file_get_contents("vf-start/VF_PROJECT.json"),true); echo $p["working_schema_version"];')" = "$EXPECTED_SCHEMA"
          grep -Fq 'OWNER_PREVIEW_RUNTIME_APPLICABILITY = N_A' vf-start/docs/release/V2.47.9_RELEASE_GATE.md
          rm -rf formal candidate
          cp -a vf-start formal
          cp -a vf-start candidate
          cp helper/scripts/_tmp_p01_v2470_formal_artifact_gate.sh /tmp/p01-v2479-artifact-gate.sh
          python3 - <<'PY'
          from pathlib import Path
          import os
          p=Path('/tmp/p01-v2479-artifact-gate.sh')
          s=p.read_text(encoding='utf-8')
          repl=[
            ('7f3492f70276d83efed371296d69709c1e991350',os.environ['FORMAL_SHA']),
            ('b70971514ccf1c6b8f3793bfc5a2200cad47dfe9',os.environ['FORMAL_SHA']),
            ('1bd95e8777e72f6cd89206ddb9daf934ad4e6b9e',os.environ['SOURCE_SHA']),
            ('33993725218',os.environ['GITHUB_RUN_ID']),
            ('BIND_PR=416','BIND_PR=754'),
            ('OWNER_AUTHORIZED_V2470_RELEASE_CHAIN_20260906','OWNER_AUTHORIZED_V2479_RELEASE_CHAIN_20260911'),
            ('V2470','V2479'),('v2470','v2479')]
          for a,b in repl:
              if a not in s: raise SystemExit('missing helper anchor '+a)
              s=s.replace(a,b)
          s=s.replace('2.47.0','__TARGET__').replace('2.46.4','2.47.8').replace('__TARGET__','2.47.9')
          old="src=src.replace(\"'release_type':'patch'\",\"'release_type':'minor'\")"
          if old not in s: raise SystemExit('release-type helper anchor missing')
          s=s.replace(old,"src=src.replace('V2.46.1 之后 Consolidated L2 Product Optimization 的正式发布版本。','V2.47.8 之后 Pending Inbox system-presentation 收口的正式发布版本。')",1)
          p.write_text(s,encoding='utf-8')
          PY
          chmod +x /tmp/p01-v2479-artifact-gate.sh
          bash /tmp/p01-v2479-artifact-gate.sh | tee /tmp/p01-v2479-artifact.log
          grep -Fx 'P01_V2479_FORMAL_ARTIFACT_GATE=PASS' /tmp/p01-v2479-artifact.log
          echo P01_V2479_REAL_UPGRADE=PASS

      - name: Stable package contract
        shell: bash
        run: |
          set -Eeuo pipefail
          ASSETS=/tmp/p01-v2479-formal-artifacts
          FULL="$ASSETS/VF-Start-V2.47.9-FULL.zip"
          UPDATE="$ASSETS/VF_Start_V2.47.9_UPDATE.zip"
          for f in "$FULL" "$FULL.sha256" "$UPDATE" "$UPDATE.sha256"; do test -f "$f"; done
          (cd "$ASSETS" && sha256sum -c VF-Start-V2.47.9-FULL.zip.sha256 && sha256sum -c VF_Start_V2.47.9_UPDATE.zip.sha256)
          test "$(unzip -p "$FULL" VERSION.txt | tr -d '\r\n ')" = "$TARGET_VERSION"
          unzip -p "$FULL" release-manifest.json >/tmp/p01-v2479-release-manifest.json
          jq -e --arg t "$TARGET_VERSION" --arg s "$SOURCE_VERSION" --arg schema "$EXPECTED_SCHEMA" '.version==$t and .source_version==$s and .schema_version==$schema and .source_schema_version==$schema and .schema_change==false and (.schema_migrations|length)==0 and .runtime_data_included==false and .seed_user_business_data_included==false and .update.backup_required==true and .update.rollback_supported==true and .update.single_hop==true and (.update.supported_from==[$s])' /tmp/p01-v2479-release-manifest.json >/dev/null
          test "$(unzip -Z1 "$UPDATE" | wc -l | tr -d ' ')" = 1
          unzip -Z1 "$UPDATE" | grep -Fxq repair-v2.47.9.php
          php "$ASSETS/repair-v2.47.9.php" --self-test | jq -e '.ok==true and .global_barrier==true and .interruption_recovery==true' >/dev/null
          ! unzip -Z1 "$FULL" | grep -Eq '(^|/)(private|backups|uploads|runtime-data)(/|$)'
          echo P01_V2479_PACKAGE_CONTRACT=PASS

      - name: Create stable tag and release
        env:
          GH_TOKEN: ${{ env.RELEASE_TOKEN }}
        shell: bash
        run: |
          set -Eeuo pipefail
          ASSETS=/tmp/p01-v2479-formal-artifacts
          gh api --method POST "repos/${PRODUCT_REPO}/git/refs" -f ref="refs/tags/${TARGET_TAG}" -f sha="$FORMAL_SHA" >/dev/null
          test "$(gh api "repos/${PRODUCT_REPO}/git/ref/tags/${TARGET_TAG}" --jq '.object.sha')" = "$FORMAL_SHA"
          cat >/tmp/p01-v2479-notes.md <<EOF
          # P01 · VF Start V2.47.9

          - R24：把待整理明确为系统收件箱 presentation，不再把它表现成普通业务分类。
          - 待整理页面明确区分当前状态、AI 建议去向与正式去向；旧 category 关系或 AI suggestion 不再冒充正式归类。
          - 资源管理侧栏按系统收件箱 / 资源库 / 业务分类 / 治理分组；确认正式去向后资源才离开 Inbox。
          - Pending Authority 继续复用既有 links.is_pending；无新分类、Shadow Table、Schema 或 Migration。
          - Exact Formal Source / Candidate: ${FORMAL_SHA}
          - Previous Stable: ${SOURCE_TAG} / ${SOURCE_SHA}
          - Schema: ${EXPECTED_SCHEMA} unchanged; Migration: NONE.
          - Fresh Install / authenticated desktop+mobile Pending Inbox / canonical Pending exit: PASS on Candidate Run ${CANDIDATE_RUN}.
          - Strict 2.47.8→2.47.9 Atomic Update / existing-data preservation / backup+rollback / Security / package contract: PASS on Run ${GITHUB_RUN_ID}.
          - OWNER_PREVIEW_RUNTIME_APPLICABILITY: N_A with reason recorded in docs/release/V2.47.9_RELEASE_GATE.md.
          - Release / Distribution do not imply Production upgraded.
          EOF
          gh release create "$TARGET_TAG" "$ASSETS/VF-Start-V2.47.9-FULL.zip" "$ASSETS/VF-Start-V2.47.9-FULL.zip.sha256" "$ASSETS/VF_Start_V2.47.9_UPDATE.zip" "$ASSETS/VF_Start_V2.47.9_UPDATE.zip.sha256" --repo "$PRODUCT_REPO" --title 'VF Start V2.47.9' --notes-file /tmp/p01-v2479-notes.md --verify-tag
          test "$(gh api "repos/${PRODUCT_REPO}/releases/tags/${TARGET_TAG}" --jq '.prerelease')" = false
          test "$(gh api "repos/${PRODUCT_REPO}/releases/tags/${TARGET_TAG}" --jq '.draft')" = false
          echo P01_V2479_STABLE_RELEASE=PASS

      - name: Independent remote byte readback
        env:
          GH_TOKEN: ${{ env.PRIVATE_READ_TOKEN }}
        shell: bash
        run: |
          set -Eeuo pipefail
          ASSETS=/tmp/p01-v2479-formal-artifacts
          test "$(gh api "repos/${PRODUCT_REPO}/git/ref/tags/${TARGET_TAG}" --jq '.object.sha')" = "$FORMAL_SHA"
          rm -rf /tmp/p01-v2479-remote
          mkdir -p /tmp/p01-v2479-remote
          gh release download "$TARGET_TAG" --repo "$PRODUCT_REPO" --dir /tmp/p01-v2479-remote
          LOCAL="$ASSETS" REMOTE=/tmp/p01-v2479-remote python3 - <<'PY'
          from pathlib import Path
          import hashlib,os
          l=Path(os.environ['LOCAL']);r=Path(os.environ['REMOTE'])
          names=['VF-Start-V2.47.9-FULL.zip','VF-Start-V2.47.9-FULL.zip.sha256','VF_Start_V2.47.9_UPDATE.zip','VF_Start_V2.47.9_UPDATE.zip.sha256']
          assert sorted(p.name for p in r.iterdir() if p.is_file())==sorted(names)
          for n in names:
              a,b=l/n,r/n
              ha=hashlib.sha256(a.read_bytes()).hexdigest();hb=hashlib.sha256(b.read_bytes()).hexdigest()
              assert a.stat().st_size==b.stat().st_size and ha==hb
              print(f'FORMAL_ASSET={n}|{a.stat().st_size}|{ha}')
          print('P01_V2479_REMOTE_BYTES=PASS')
          PY

      - name: Final formal verdict
        run: |
          echo PUBLIC_AUTHORITY_INSTALL=PASS_FROM_CANDIDATE_RUN_34662954221
          echo PUBLIC_AUTHORITY_SECURITY=PASS
          echo PUBLIC_AUTHORITY_TESTING=PASS
          echo PUBLIC_AUTHORITY_UPGRADE=PASS
          echo PUBLIC_AUTHORITY_DATA_SCHEMA=PASS
          echo PUBLIC_AUTHORITY_GIT=PASS
          echo PUBLIC_AUTHORITY_UA_UI=PASS_FROM_CANDIDATE_RUN_34662954221
          echo OWNER_PREVIEW_RUNTIME_APPLICABILITY=N_A
          echo OWNER_REAL_USE=POST_RELEASE_PENDING
          echo P01_V2479_FORMAL_RELEASE_GATE=PASS
          echo STABLE_DISTRIBUTION_UPDATED=false
          echo PRODUCTION=false

      - name: Cleanup
        if: always()
        run: rm -rf vf-start baseline helper template proven formal candidate /tmp/p01-v2479-*
