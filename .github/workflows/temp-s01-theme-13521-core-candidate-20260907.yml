name: S01 Theme 1.35.21 Core Candidate
on:
  push:
    branches:
      - temp/s01-update-center-id-closure-20260907
    paths:
      - .github/workflows/temp-s01-theme-13521-core-candidate-20260907.yml
permissions:
  contents: read
jobs:
  prepare:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      READ_TOKEN: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
      WRITE_TOKEN: ${{ secrets.VF_RELEASE_WRITE_TOKEN }}
      CORE_REPO: llhzx2018/core-updates
      CORE_BRANCH: work/s01-theme-13521-distribution-20260907
      CORE_BASE: e37c3d9e753d32e97a4de7277405ebd0d59e1a94
      THEME_SHA: e27aebdc3d1674711a2ec6e9e113119bf2d9071a
    steps:
      - uses: actions/checkout@v4
      - name: Prepare exact C01 distribution candidate
        env:
          GH_TOKEN: ${{ env.WRITE_TOKEN }}
        run: bash scripts/temp_s01_theme_13521_core_candidate_20260907.sh
