name: P04 V2.5.4 E2E Modal Focus Frame Patch

on:
  push:
    paths:
      - '.github/workflows/p04-v254-e2e-modal-focus-frame-patch.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply modal focus-frame assertion patch
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/p04-v254-modal-focus-frame-patch.py
          node --check scripts/p04-v254-browser-e2e.mjs
          git diff --check
      - name: Commit
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'VictorForge'
          git config user.email 'llhzx2018@gmail.com'
          git add scripts/p04-v254-browser-e2e.mjs
          git commit -m 'runner(P04): await V2.5.4 modal focus restoration frame'
          git push origin HEAD:main
