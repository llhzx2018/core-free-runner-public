name: TEMP skill-book V4.8 Final Closure Gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'TEMP V4.8 branch frozen; PR will close without merge.'
