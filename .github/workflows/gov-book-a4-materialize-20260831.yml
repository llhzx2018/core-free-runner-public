name: GOV-BOOK Binary Materialize Retired 20260901

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Retired workflow tombstone
        shell: bash
        run: |
          set -Eeuo pipefail
          echo 'WORKFLOW_STATE=RETIRED'
          echo 'AUTO_TRIGGER=DISABLED'
          echo 'GOV_BOOK_CLOSURE_COMMIT=3648d84fe0b95dc522d2e9c3f2e8a9ace4bc1b23'
          echo 'HISTORICAL_RUNS_RETAINED=YES'
          echo 'MATERIALIZATION_WRITE_PATH=DISABLED'
