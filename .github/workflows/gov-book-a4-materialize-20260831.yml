name: GOV-BOOK B2 Exact Binary Materialize 20260831

on:
  push:
    branches:
      - task/gov-book-a4-materialize-20260831
    paths:
      - '.github/workflows/gov-book-a4-materialize-20260831.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: gov-book-binary-materialize-20260831
  cancel-in-progress: false

jobs:
  materialize:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      PRIVATE_SOURCE_SHA: a7bfb7524db86016e6d70d0942f90d7d285a4eb3
      TARGET_DEVELOP_SHA: 9d52d43c37150497acf6e6b54d6f4006ef848665
      TARGET_MAIN_SHA: e89b0b98dd657e19187940e859fe9e5161df750e
      ZIP_SHA256: 0a546b3dff12e040a56047a0cb3c5bebec666275e8fd32460e5031fdb90d27be
      ZIP_SIZE: '228941'
      TARGET_PATH: downloads/B2/v1.0.0/B2_付费流量与竞争情报_v1.0.0_中文版_PASS.zip
      VF_PRIVATE_READ_TOKEN: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
      VF_RELEASE_WRITE_TOKEN: ${{ secrets.VF_RELEASE_WRITE_TOKEN }}

    steps:
      - name: Require registered VF capabilities
        shell: bash
        run: |
          set -Eeuo pipefail
          test -n "$VF_PRIVATE_READ_TOKEN"
          test -n "$VF_RELEASE_WRITE_TOKEN"
          echo 'REGISTERED_VF_CAPABILITIES=PASS'

      - name: Checkout corrected exact private transport
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          repository: llhzx2018/core-free-runner-private
          ref: ${{ env.PRIVATE_SOURCE_SHA }}
          token: ${{ env.VF_PRIVATE_READ_TOKEN }}
          persist-credentials: false
          fetch-depth: 1
          path: private-input

      - name: Rebuild exact B2 ZIP with full transport gates
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 - <<'PY'
          import base64, hashlib, json, lzma, os, pathlib, subprocess, zipfile
          q = pathlib.Path('private-input/queue/gov-book-20260831/B2')
          spec = json.loads((q/'READY.json').read_text(encoding='utf-8'))
          assert spec['book_id'] == 'B2'
          assert spec['payload_sha256'] == os.environ['ZIP_SHA256']
          assert spec['payload_size_bytes'] == int(os.environ['ZIP_SIZE'])
          assert spec['expected_target_head'] == os.environ['TARGET_DEVELOP_SHA']
          assert spec['expected_main_head'] == os.environ['TARGET_MAIN_SHA']
          assert spec['target_path'] == os.environ['TARGET_PATH']
          pieces=[]
          for c in spec['chunks']:
              p=q/c['path']
              data=p.read_bytes()
              assert len(data)==c['size']
              git_path=f"queue/gov-book-20260831/B2/{c['path']}"
              actual=subprocess.check_output(['git','-C','private-input','rev-parse',f'HEAD:{git_path}'],text=True).strip()
              assert actual==c['git_blob'], (c['path'],actual,c['git_blob'])
              pieces.append(data.decode('ascii'))
          encoded=''.join(pieces).encode('ascii')
          assert len(encoded)==spec['encoded_size_bytes']
          packed=base64.b64decode(encoded,validate=True)
          assert len(packed)==spec['xz_size_bytes']
          assert hashlib.sha256(packed).hexdigest()==spec['xz_sha256']
          raw=lzma.decompress(packed)
          assert len(raw)==int(os.environ['ZIP_SIZE'])
          assert hashlib.sha256(raw).hexdigest()==os.environ['ZIP_SHA256']
          out=pathlib.Path(os.environ['RUNNER_TEMP'])/'B2.zip'
          out.write_bytes(raw)
          with zipfile.ZipFile(out) as z:
              assert z.testzip() is None
          print('B2_TRANSPORT_EXACT=PASS')
          print('B2_PAYLOAD_SHA256=PASS')
          print('B2_ZIP_CRC=PASS')
          PY

      - name: Checkout exact GOV-BOOK develop
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          repository: llhzx2018/gov-book
          ref: ${{ env.TARGET_DEVELOP_SHA }}
          token: ${{ env.VF_RELEASE_WRITE_TOKEN }}
          persist-credentials: true
          fetch-depth: 0
          path: gov-book

      - name: Hard-lock main and develop then write B2 only
        working-directory: gov-book
        shell: bash
        run: |
          set -Eeuo pipefail
          git fetch origin main develop
          ACTUAL_MAIN="$(git rev-parse origin/main)"
          ACTUAL_DEVELOP="$(git rev-parse origin/develop)"
          echo "ACTUAL_MAIN=$ACTUAL_MAIN"
          echo "ACTUAL_DEVELOP=$ACTUAL_DEVELOP"
          test "$ACTUAL_MAIN" = "$TARGET_MAIN_SHA"
          test "$ACTUAL_DEVELOP" = "$TARGET_DEVELOP_SHA"
          test ! -e "$TARGET_PATH"
          mkdir -p "$(dirname "$TARGET_PATH")"
          cp "$RUNNER_TEMP/B2.zip" "$TARGET_PATH"
          python3 - <<'PY'
          import hashlib,os,pathlib,zipfile
          p=pathlib.Path(os.environ['TARGET_PATH'])
          assert p.stat().st_size==int(os.environ['ZIP_SIZE'])
          assert hashlib.sha256(p.read_bytes()).hexdigest()==os.environ['ZIP_SHA256']
          with zipfile.ZipFile(p) as z:
              assert z.testzip() is None
          PY
          git add -f -- "$TARGET_PATH"
          changed="$(git -c core.quotepath=false diff --cached --name-only)"
          test "$changed" = "$TARGET_PATH"
          git config user.name 'VF Public Runner'
          git config user.email 'runner@users.noreply.github.com'
          git commit -m 'content(book): materialize exact B2 Chinese download'
          git push origin HEAD:develop

      - name: Exact remote readback
        working-directory: gov-book
        shell: bash
        run: |
          set -Eeuo pipefail
          git fetch origin main develop
          REMOTE_MAIN="$(git rev-parse origin/main)"
          REMOTE_DEVELOP="$(git rev-parse origin/develop)"
          test "$REMOTE_MAIN" = "$TARGET_MAIN_SHA"
          git show "origin/develop:$TARGET_PATH" > "$RUNNER_TEMP/B2-remote.zip"
          python3 - <<'PY'
          import hashlib,os,pathlib,zipfile
          p=pathlib.Path(os.environ['RUNNER_TEMP'])/'B2-remote.zip'
          assert p.stat().st_size==int(os.environ['ZIP_SIZE'])
          assert hashlib.sha256(p.read_bytes()).hexdigest()==os.environ['ZIP_SHA256']
          with zipfile.ZipFile(p) as z:
              assert z.testzip() is None
          print('B2_REMOTE_EXACT_READBACK=PASS')
          print('B2_REMOTE_ZIP_CRC=PASS')
          PY
          echo "GOV_BOOK_DEVELOP_RESULT=$REMOTE_DEVELOP"
          echo 'MAIN_UNCHANGED=PASS'
          echo 'PRIVATE_SOURCE_PERSISTED_PUBLIC=NO'
