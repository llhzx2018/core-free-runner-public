name: P02 V2.5.105 Discovery Verify

on:
  push:
    branches:
      - run/p02-v25105-final-alignment-20260924
    paths:
      - '.github/workflows/p02-v25105-discovery-verify.yml'
      - 'requests/p02-v25105-discovery-verify.sh'

permissions:
  contents: read

jobs:
  discovery:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout runner carrier
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          path: runner

      - name: Checkout exact merged P02 main
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: llhzx2018/vf-library
          ref: c103134ba9e23f83d68c9676cdf892fb2629e8f8
          token: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
          persist-credentials: false
          fetch-depth: 1
          path: product

      - name: Install PHP runtime
        run: |
          set -Eeuo pipefail
          sudo apt-get update
          sudo apt-get install -y php-cli curl

      - name: Verify public stable discovery
        run: |
          set -Eeuo pipefail
          bash runner/requests/p02-v25105-discovery-verify.sh
