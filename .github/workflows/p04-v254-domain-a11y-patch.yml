name: P04 V2.5.4 Domain Accessibility Patch

on:
  push:
    paths:
      - '.github/workflows/p04-v254-domain-a11y-patch.yml'

permissions:
  contents: read

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: llhzx2018/vf-infra
          ref: maintenance/p04-v2.5.4-full-backend-ui-20260816
          token: ${{ secrets.VF_RELEASE_WRITE_TOKEN }}
          fetch-depth: 0
          path: vf-infra
      - name: Apply exact domains accessibility fixes
        shell: bash
        run: |
          set -euo pipefail
          cp scripts/p04-v254-domain-a11y-patch.py vf-infra/.v254-domain-a11y.py
          cd vf-infra
          python3 .v254-domain-a11y.py
          rm .v254-domain-a11y.py
          node --check public/assets/v254-ui.js
          git diff --check
          test "$(git diff --name-only)" = $'public/assets/v254-ui.css\npublic/assets/v254-ui.js'
      - name: Commit and push
        shell: bash
        working-directory: vf-infra
        run: |
          set -euo pipefail
          git config user.name 'VictorForge'
          git config user.email 'llhzx2018@gmail.com'
          git add public/assets/v254-ui.css public/assets/v254-ui.js
          git commit -m 'fix(P04): close V2.5.4 domains accessibility gaps'
          git push origin HEAD:maintenance/p04-v2.5.4-full-backend-ui-20260816
