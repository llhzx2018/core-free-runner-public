name: P02 V2.5.17 Real App Browser Gate V2

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/p02-v2517-browser-gate-v2.yml'

permissions:
  contents: read

jobs:
  browser:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          persist-credentials: false
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          repository: llhzx2018/vf-library
          ref: maintenance/v2.5.17-library-model-convergence
          token: '${{ secrets.VF_RELEASE_WRITE_TOKEN }}'
          path: product
          fetch-depth: 0
          persist-credentials: false
      - name: Build, install and verify source
        working-directory: product
        shell: bash
        run: |
          set -Eeuo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq php8.3-cli php8.3-sqlite3 php8.3-zip sqlite3 >/dev/null
          test "$(tr -d '\r\n' < VERSION)" = 2.5.17
          test "$(jq -r .version SOURCE_MANIFEST.json)" = 2.5.17
          test "$(jq -r .schema SOURCE_MANIFEST.json)" = 2401
          test "$(jq -r .runtime_source_file_count SOURCE_MANIFEST.json)" = 74
          python3 - <<'PY'
          import hashlib,json,pathlib
          m=json.load(open('SOURCE_MANIFEST.json',encoding='utf-8'));assert len(m['entries'])==74
          for e in m['entries']:
              b=pathlib.Path(e['repo_path']).read_bytes();h=hashlib.sha256(b).hexdigest();assert len(b)==e['bytes'] and h==e['sha256']==e['repo_sha256'],e['repo_path']
          print('SOURCE_MANIFEST_74_OF_74=PASS')
          PY
          ROOT="$RUNNER_TEMP/p02-v2517-app"; SITE="$ROOT/site"; mkdir -p "$SITE"; echo "$SITE" > "$RUNNER_TEMP/site-path"
          bash scripts/build-deploy-tree.sh "$SITE" >/dev/null
          test "$(cat "$SITE/VERSION.txt")" = 2.5.17
          php -S 127.0.0.1:18739 -t "$SITE" >"$RUNNER_TEMP/p02-v2517-app.log" 2>&1 & echo $! > "$RUNNER_TEMP/php-pid"
          sleep 1
          curl -fsS -c "$RUNNER_TEMP/setup-cookies" http://127.0.0.1:18739/setup.php > "$RUNNER_TEMP/setup.html"
          CSRF="$(python3 - "$RUNNER_TEMP/setup.html" <<'PY'
          import re,sys
          s=open(sys.argv[1],encoding='utf-8').read();m=re.search(r'name="setup_csrf" value="([^"]+)"',s);print(m.group(1) if m else '')
          PY
          )"; test -n "$CSRF"
          curl -sS -D "$RUNNER_TEMP/setup-headers" -o /dev/null -b "$RUNNER_TEMP/setup-cookies" \
            -H 'Origin: http://127.0.0.1:18739' -H 'Referer: http://127.0.0.1:18739/setup.php' \
            --data-urlencode "setup_csrf=$CSRF" --data-urlencode 'password=P02-V2517-Test-Only!' --data-urlencode 'password_confirm=P02-V2517-Test-Only!' \
            http://127.0.0.1:18739/setup.php
          grep -Eq '^HTTP/.* 303' "$RUNNER_TEMP/setup-headers"
          echo FRESH_INSTALL=PASS
      - name: Real Chromium product semantics
        working-directory: product
        shell: bash
        run: |
          set -Eeuo pipefail
          PID="$(cat "$RUNNER_TEMP/php-pid")"; trap 'kill "$PID" || true; echo "--- PHP LOG ---"; tail -160 "$RUNNER_TEMP/p02-v2517-app.log" || true' EXIT
          npm -g ls playwright >/dev/null 2>&1 || npm i -g playwright@1.55.0 >/dev/null
          playwright install chromium >/dev/null
          cat > "$RUNNER_TEMP/p02-v2517-app.cjs" <<'JS'
          const {chromium}=require('playwright');
          const assert=(ok,msg)=>{if(!ok)throw new Error(msg)};const sleep=ms=>new Promise(r=>setTimeout(r,ms));
          (async()=>{
            const browser=await chromium.launch({headless:true});
            const page=await browser.newPage({viewport:{width:1440,height:1000}});const base='http://127.0.0.1:18739';
            await page.goto(base+'/',{waitUntil:'networkidle'});
            await page.click('[data-open-login]');await page.fill('input[name="password"]','P02-V2517-Test-Only!');await page.click('#loginSubmit');
            await page.waitForSelector('#allCount',{state:'visible'});await page.waitForFunction(()=>document.querySelector('#accountBtn')?.textContent.includes('退出'));
            const ia=await page.evaluate(()=>({q:getComputedStyle(document.querySelector('.side-item[data-mode="quick"]')).display,a:getComputedStyle(document.querySelector('.side-item[data-mode="article"]')).display,all:document.querySelector('.side-item[data-mode="all"]')?.innerText||'',fav:document.querySelector('.side-item[data-mode="favorite"]')?.innerText||'',recent:document.querySelector('.side-item[data-mode="recent"]')?.innerText||''}));
            assert(ia.q==='none'&&ia.a==='none','LEGACY_PRIMARY_NAV_VISIBLE');assert(ia.all.includes('全部内容')&&ia.fav.includes('我的收藏')&&ia.recent.includes('最近使用'),'PRIMARY_IA_MISSING');
            for(const legacy of ['quick','article']){await page.evaluate(v=>localStorage.setItem('vftb-mode',v),legacy);await page.reload({waitUntil:'networkidle'});await page.waitForSelector('#allCount',{state:'visible'});await page.waitForFunction(()=>localStorage.getItem('vftb-mode')==='all');}
            const c=await page.evaluate(async()=>{const s=await fetch('/api.php?action=session').then(r=>r.json()),csrf=s.csrf;const post=async(a,b)=>{const r=await fetch('/api.php?action='+a,{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify(b)}),j=await r.json();if(!r.ok||j.ok===false)throw Error(a+JSON.stringify(j));return j};const cat=await post('category_save',{name:'V2517 浏览器验证'}),base={category_id:cat.id,description:'',tags:'',aliases:'',is_favorite:false,is_pinned:false,status:'active',attachment_ids:[]};const q=await post('content_save',{...base,title:'V2517 普通资料',content:'普通资料正文',content_mode:'quick',content_format:'plain',primary_action:'copy'}),a=await post('content_save',{...base,title:'V2517 长文资料',content:'# 长文资料\n\n真实阅读测试。',content_mode:'article',content_format:'markdown',primary_action:'read'}),e=await post('content_save',{...base,title:'V2517 编辑资料',content:'编辑测试',content_mode:'quick',content_format:'plain',primary_action:'copy'});const boot=await fetch('/api.php?action=app_bootstrap').then(r=>r.json()),recent=await fetch('/api.php?action=content_list&mode=recent&status=active&page=1&page_size=20').then(r=>r.json());return{q:q.id,a:a.id,e:e.id,setting:Number(boot.data.settings.article_open_counts_recent||0),total:Number(recent.data.pagination.total||0)}});
            assert(c.setting===0,'LEGACY_ARTICLE_OPEN_SETTING_NOT_ZERO');assert(c.total===0,'RECENT_NOT_EMPTY_INITIAL');
            await page.reload({waitUntil:'networkidle'});await page.click(`[data-open="${c.q}"]`);await sleep(950);let r=await page.evaluate(()=>fetch('/api.php?action=content_list&mode=recent&status=active&page=1&page_size=20').then(x=>x.json()));assert(Number(r.data.pagination.total)===1&&Number(r.data.items[0].id)===Number(c.q)&&r.data.items[0].last_opened_at,'QUICK_OPEN_RECENT_FAIL');
            await sleep(1100);await page.goto(base+'/',{waitUntil:'networkidle'});await page.click(`[data-open="${c.a}"]`);await sleep(950);r=await page.evaluate(()=>fetch('/api.php?action=content_list&mode=recent&status=active&page=1&page_size=20').then(x=>x.json()));assert(Number(r.data.pagination.total)===2&&Number(r.data.items[0].id)===Number(c.a)&&r.data.items[0].last_opened_at,'ARTICLE_OPEN_RECENT_FAIL');
            await sleep(1100);await page.goto(base+'/',{waitUntil:'networkidle'});await page.click(`[data-item-more="${c.e}"]`);await page.click('[data-edit]');await page.waitForSelector('#contentForm',{state:'visible'});await sleep(950);const hidden=await page.evaluate(()=>{const s=document.querySelector('select[name="content_mode"]');return !!(s&&s.closest('.field')&&s.closest('.field').hidden)});assert(hidden,'CONTENT_MODE_VISIBLE');r=await page.evaluate(()=>fetch('/api.php?action=content_list&mode=recent&status=active&page=1&page_size=20').then(x=>x.json()));assert(Number(r.data.pagination.total)===3&&Number(r.data.items[0].id)===Number(c.e),'EDIT_RECENT_FAIL');
            await page.goto(base+'/',{waitUntil:'networkidle'});await page.waitForFunction(()=>Number(document.querySelector('#recentCount')?.textContent||0)===3);await page.click('.side-item[data-mode="recent"]');await page.waitForFunction(()=>document.body.innerText.includes('最近打开、阅读、编辑、复制或调用过的资料'));
            await page.goto(base+'/',{waitUntil:'networkidle'});await page.click('#addContentMenuBtn');await sleep(100);const body=await page.evaluate(()=>document.body.innerText);assert(body.includes('新建资料')&&body.includes('新建长文'),'CREATE_LABEL_CONVERGENCE_FAIL');
            const mobile=await browser.newPage({viewport:{width:390,height:844}});await mobile.goto(base+'/',{waitUntil:'networkidle'});const ov=await mobile.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth);assert(ov<=1,'MOBILE_OVERFLOW_'+ov);
            await browser.close();console.log('PRIMARY_LIBRARY_IA=PASS');console.log('LEGACY_MODE_FALLBACK=PASS');console.log('QUICK_OPEN_RECENT=PASS');console.log('ARTICLE_OPEN_RECENT_SETTING_OFF=PASS');console.log('EDIT_RECENT=PASS');console.log('RECENT_ORDERING=PASS');console.log('VISIBLE_TERMINOLOGY_CONVERGENCE=PASS');console.log('MOBILE_390_OVERFLOW=NONE');console.log('REAL_CHROMIUM_V2517=PASS');
          })().catch(e=>{console.error(e&&e.stack||e);process.exit(1)});
          JS
          NODE_PATH="$(npm root -g)" node "$RUNNER_TEMP/p02-v2517-app.cjs"
          echo SCHEMA=2401
          echo PRODUCTION_WRITE=NO
# trigger browser gate v2
