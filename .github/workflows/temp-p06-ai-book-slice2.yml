name: P06 VF Press V0.1.10 Temporary Formal Release

on:
  pull_request:
    paths:
      - .github/workflows/temp-p06-ai-book-slice2.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: p06-v0110-formal-release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      SOURCE_REPO: llhzx2018/vf-press
      SOURCE_SHA: 0ef381fb8ffd945ba314183d558987e17b17ae38
      VERSION: 0.1.10
      TAG: v0.1.10
      CORE_REPO: llhzx2018/core-updates
    steps:
      - name: Require registered release capabilities
        shell: bash
        env:
          READ_TOKEN: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
          WRITE_TOKEN: ${{ secrets.VF_RELEASE_WRITE_TOKEN }}
        run: |
          set -Eeuo pipefail
          test -n "$READ_TOKEN"
          test -n "$WRITE_TOKEN"
          echo PRIVATE_READ_CAPABILITY=PASS
          echo RELEASE_WRITE_CAPABILITY=PASS

      - name: Checkout exact release candidate source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          repository: ${{ env.SOURCE_REPO }}
          ref: ${{ env.SOURCE_SHA }}
          token: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
          persist-credentials: false
          fetch-depth: 1
          path: source

      - name: Checkout core-updates channel
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          repository: ${{ env.CORE_REPO }}
          ref: main
          token: ${{ secrets.VF_RELEASE_WRITE_TOKEN }}
          fetch-depth: 0
          path: core-updates

      - name: Prepare release runtime
        shell: bash
        run: |
          set -Eeuo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y php-cli php-sqlite3 php-mbstring php-zip php-curl sqlite3 composer >/dev/null
          php -v
          composer --version
          command -v gh
          echo MACHINE_RUNTIME=PASS

      - name: Exact source and authority release gate
        working-directory: source
        shell: bash
        run: |
          set -Eeuo pipefail
          test "$(git rev-parse HEAD)" = "$SOURCE_SHA"
          test "$(tr -d '\r\n' < VERSION)" = "$VERSION"
          test "$(jq -r .version VF_PROJECT.json)" = "$VERSION"
          test "$(jq -r .schema VF_PROJECT.json)" = '3'
          test "$(jq -r .current_phase VF_PROJECT.json)" = 'V0_1_10_READER_STUDIO_UX_REMEDIATION_CANDIDATE'
          test "$(jq -r .next_gate VF_PROJECT.json)" = 'EXACT_SOURCE_RUNNER_THEN_FORMAL_RELEASE'
          test "$(jq -r .release_gates.owner_preview_runtime_applicability VF_PROJECT.json)" = 'N/A'
          test -n "$(jq -r .release_gates.owner_preview_runtime_reason VF_PROJECT.json)"
          test "$(jq -r .release_gates.owner_formal_release_approval VF_PROJECT.json)" = 'PASS'
          test "$(jq -r .release_gates.six_center_admin_shell VF_PROJECT.json)" = 'PASS_MACHINE_EXACT_SOURCE_RUN_32725409369'
          test "$(jq -r .product_gates.quality_center VF_PROJECT.json)" = 'PASS_MACHINE_EXACT_SOURCE'
          test "$(jq -r .product_gates.system_center_controller_split VF_PROJECT.json)" = 'PASS_MACHINE_EXACT_SOURCE'
          test "$(jq -r .lifecycle VF_PROJECT.json)" = 'CANDIDATE'
          test "$(jq -r .release_gates.reader_studio_ux_v0_1_10 VF_PROJECT.json)" = 'CANDIDATE_EXACT_SOURCE_RUN_PENDING'
          test "$(jq -r .release_gates.formal_release_v0_1_10 VF_PROJECT.json)" = 'PENDING_EXACT_SOURCE_RUN'
          test "$(jq -r .production_update_state.observed_runtime_version VF_PROJECT.json)" = '0.1.9'
          test "$(jq -r .production_update_state.demo_data_write_authorization VF_PROJECT.json)" = 'OWNER_AUTHORIZED_2026_08_24'
          echo EXACT_RELEASE_SOURCE=PASS
          echo AUTHORITY_RELEASE_GATE=PASS
          echo PRE_RELEASE_AUTHORITY_DRIFT_GATE=PASS
          echo PRODUCTION_WRITE=0

      - name: Composer, PHP syntax and complete regression
        working-directory: source
        shell: bash
        run: |
          set -Eeuo pipefail
          composer validate --strict
          composer dump-autoload --no-dev --optimize --no-interaction
          find src bin public -type f -name '*.php' -print0 | xargs -0 -n1 php -l >/dev/null
          rm -rf storage dist storage-restore-*
          php bin/migrate.php | grep -Fq MIGRATION_PASS
          test "$(sqlite3 storage/app.db 'SELECT MAX(version) FROM schema_migrations;')" = '3'
          php bin/self-test.php | grep -Fq FOUNDATION_SELF_TEST_PASS
          php bin/security-self-test.php | grep -Fq SECURITY_SELF_TEST_PASS
          php bin/research-self-test.php | grep -Fq RESEARCH_SELF_TEST_PASS
          php bin/publication-self-test.php | grep -Fq PUBLICATION_MANUSCRIPT_SELF_TEST=PASS
          php bin/markdown-import-self-test.php | grep -Fq MARKDOWN_IMPORT_SELF_TEST=PASS
          php bin/reader-self-test.php | tee /tmp/p06-v0110-reader-test
          grep -Fq PUBLISHER_READER_SELF_TEST=PASS /tmp/p06-v0110-reader-test
          grep -Fq LEGACY_DEMO_MARKDOWN_REPAIR=PASS /tmp/p06-v0110-reader-test
          php bin/reader-ux-self-test.php | tee /tmp/p06-v0110-reader-ux-test
          grep -Fq READER_UX_SELF_TEST=PASS /tmp/p06-v0110-reader-ux-test
          grep -Fq DIRECT_PRIVATE_READING=PASS /tmp/p06-v0110-reader-ux-test
          grep -Fq FOCUSED_STUDIO=PASS /tmp/p06-v0110-reader-ux-test
          grep -Fq READABLE_DEFAULTS=PASS /tmp/p06-v0110-reader-ux-test
          php bin/edition-self-test.php | grep -Fq EDITION_SELF_TEST=PASS
          php bin/living-publishing-self-test.php | grep -Fq LIVING_PUBLISHING_SELF_TEST=PASS
          php bin/operations-self-test.php | grep -Fq OPERATIONS_SELF_TEST=PASS
          php bin/operations-ui-self-test.php | grep -Fq OPERATIONS_UI_SELF_TEST=PASS
          php bin/admin-shell-book-overview-self-test.php | grep -Fq ADMIN_SHELL_BOOK_OVERVIEW_SELF_TEST=PASS
          php bin/admin-shell-unification-self-test.php | grep -Fq ADMIN_SHELL_UNIFICATION_SELF_TEST=PASS
          php bin/book-outline-self-test.php | grep -Fq BOOK_OUTLINE_SELF_TEST=PASS
          php bin/demo-data-self-test.php | tee /tmp/p06-v0110-demo-test
          grep -Fq DEMO_DATA_INSTALL=PASS /tmp/p06-v0110-demo-test
          grep -Fq DEMO_DATA_IDEMPOTENT=PASS /tmp/p06-v0110-demo-test
          grep -Fq DEMO_CANONICAL_MARKDOWN_READBACK=PASS /tmp/p06-v0110-demo-test
          grep -Fq DEMO_DATA_ISOLATED_REMOVE=PASS /tmp/p06-v0110-demo-test
          php bin/public-asset-self-test.php | grep -Fq PUBLIC_ASSET_HTTP_SELF_TEST=PASS
          php bin/job-self-test.php | grep -Fq JOB_QUEUE_SELF_TEST=PASS
          php bin/update-self-test.php | grep -Fq UPDATE_SELF_TEST=PASS
          php -r '$pdo=new PDO("sqlite:storage/app.db"); if($pdo->query("PRAGMA integrity_check")->fetchColumn()!=="ok") exit(1); if($pdo->query("PRAGMA foreign_key_check")->fetchAll()!==[]) exit(2);'
          rm -rf storage dist storage-restore-*
          test -z "$(git status --short --untracked-files=all)"
          echo EXACT_SOURCE_FULL_REGRESSION=PASS
          echo SIX_CENTER_ADMIN_SHELL=PASS
          echo PRODUCT_REPOSITORY_CLEAN=PASS

      - name: Build exact formal assets
        working-directory: source
        shell: bash
        run: |
          set -Eeuo pipefail
          OUT=/tmp/p06-v0110-release
          rm -rf "$OUT"
          mkdir -p "$OUT"
          php bin/build-release.php "$OUT" | tee /tmp/p06-v0110-release-build
          grep -Fq RELEASE_BUILD=PASS /tmp/p06-v0110-release-build
          (cd "$OUT" && sha256sum -c SHA256SUMS.txt)
          FULL="$OUT/VF_Press_V0.1.10_FULL.zip"
          UPDATE="$OUT/VF_Press_V0.1.10_UPDATE.zip"
          test -s "$FULL" && test -s "$UPDATE"
          test "$(jq -r .version "$OUT/RELEASE_INFO.json")" = "$VERSION"
          test "$(jq -r .schema "$OUT/RELEASE_INFO.json")" = '3'
          unzip -p "$UPDATE" VF_UPDATE.json >/tmp/p06-v0110-update.json
          test "$(jq -r .target_version /tmp/p06-v0110-update.json)" = "$VERSION"
          unzip -l "$UPDATE" | grep -Fq 'payload/src/Http/Studio/QualityController.php'
          unzip -l "$UPDATE" | grep -Fq 'payload/src/Http/Studio/AdminShell.php'
          unzip -l "$UPDATE" | grep -Fq 'payload/bin/admin-shell-unification-self-test.php'
          unzip -l "$UPDATE" | grep -Fq 'payload/src/Application/Demo/DemoDataService.php'
          unzip -l "$UPDATE" | grep -Fq 'payload/bin/demo-data-self-test.php'
          unzip -l "$UPDATE" | grep -Fq 'payload/public/assets/reader.css'
          unzip -l "$UPDATE" | grep -Fq 'payload/public/assets/reader.js'
          unzip -l "$UPDATE" | grep -Fq 'payload/src/Support/MarkdownTextNormalizer.php'
          unzip -l "$UPDATE" | grep -Fq 'payload/bin/reader-ux-self-test.php'
          echo "FULL_BYTES=$(stat -c%s "$FULL")" >> "$GITHUB_ENV"
          echo "FULL_SHA=$(sha256sum "$FULL" | awk '{print $1}')" >> "$GITHUB_ENV"
          echo "UPDATE_BYTES=$(stat -c%s "$UPDATE")" >> "$GITHUB_ENV"
          echo "UPDATE_SHA=$(sha256sum "$UPDATE" | awk '{print $1}')" >> "$GITHUB_ENV"
          echo FORMAL_ASSET_BUILD=PASS

      - name: Publish and remote-readback formal GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.VF_RELEASE_WRITE_TOKEN }}
        run: |
          set -Eeuo pipefail
          OUT=/tmp/p06-v0110-release
          NOTES=/tmp/p06-v0110-notes.md
          printf '%s\\n' \
            '## VF Press V0.1.10 · 专注写作与可持续阅读' \
            '' \
            '- 私人开始阅读直接进入正文，不再经过出版工作预览。' \
            '- Studio 收敛为单章专注写作，并直接切换当前章节 Reader。' \
            '- Reader 默认 18px、1.8 行距、700px 暖色版心，并统一公开与私人阅读规则。' \
            '- 修复演示正文转义换行与重复章节标题，不改写 Accepted Canonical Markdown。' \
            '- Schema 保持 3；未配置 AI Provider 时导入、手工编辑与私人阅读继续可用。' \
            '' \
            '本发布获得 OWNER 正式发布授权；Production 更新仍由独立 Gate 与回读证明。' > "$NOTES"

          if gh release view "$TAG" --repo "$SOURCE_REPO" >/dev/null 2>&1; then
            TAG_SHA=$(gh api "repos/$SOURCE_REPO/git/ref/tags/$TAG" --jq .object.sha)
            test "$TAG_SHA" = "$SOURCE_SHA"
            gh release upload "$TAG" "$OUT/VF_Press_V0.1.10_FULL.zip" "$OUT/VF_Press_V0.1.10_UPDATE.zip" "$OUT/SHA256SUMS.txt" "$OUT/RELEASE_INFO.json" --repo "$SOURCE_REPO" --clobber
          else
            gh release create "$TAG" "$OUT/VF_Press_V0.1.10_FULL.zip" "$OUT/VF_Press_V0.1.10_UPDATE.zip" "$OUT/SHA256SUMS.txt" "$OUT/RELEASE_INFO.json" --repo "$SOURCE_REPO" --target "$SOURCE_SHA" --title "VF Press V0.1.10 · Focused Studio & Readable Reader" --notes-file "$NOTES"
          fi

          RELEASE_JSON=$(gh api "repos/$SOURCE_REPO/releases/tags/$TAG")
          test "$(jq -r .draft <<<"$RELEASE_JSON")" = false
          test "$(jq -r .prerelease <<<"$RELEASE_JSON")" = false
          test "$(jq -r .tag_name <<<"$RELEASE_JSON")" = "$TAG"
          test "$(gh api "repos/$SOURCE_REPO/git/ref/tags/$TAG" --jq .object.sha)" = "$SOURCE_SHA"

          READBACK=/tmp/p06-v0110-readback
          rm -rf "$READBACK"
          mkdir -p "$READBACK"
          gh release download "$TAG" --repo "$SOURCE_REPO" --dir "$READBACK"
          test "$(stat -c%s "$READBACK/VF_Press_V0.1.10_FULL.zip")" = "$FULL_BYTES"
          test "$(sha256sum "$READBACK/VF_Press_V0.1.10_FULL.zip" | awk '{print $1}')" = "$FULL_SHA"
          test "$(stat -c%s "$READBACK/VF_Press_V0.1.10_UPDATE.zip")" = "$UPDATE_BYTES"
          test "$(sha256sum "$READBACK/VF_Press_V0.1.10_UPDATE.zip" | awk '{print $1}')" = "$UPDATE_SHA"
          (cd "$READBACK" && sha256sum -c SHA256SUMS.txt)
          echo "RELEASE_URL=$(jq -r .html_url <<<"$RELEASE_JSON")" >> "$GITHUB_ENV"
          echo FORMAL_RELEASE_REMOTE_READBACK=PASS

      - name: Publish core-updates P06 manifest
        working-directory: core-updates
        shell: bash
        run: |
          set -Eeuo pipefail
          RELEASED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          jq -n --arg version "$VERSION" --arg tag "$TAG" --arg sha "$UPDATE_SHA" --arg released_at "$RELEASED_AT" --arg source_sha "$SOURCE_SHA" --argjson bytes "$UPDATE_BYTES" '{
            schema_version:"1.0",
            project_id:"P06",
            component_id:"APP",
            enabled:true,
            target_version:$version,
            update_type:"ATOMIC",
            from_versions:["0.1.9"],
            schema_from:"3",
            schema_to:"3",
            repository:"llhzx2018/vf-press",
            release_tag:$tag,
            asset_name:"VF_Press_V0.1.10_UPDATE.zip",
            asset_bytes:$bytes,
            asset_sha256:$sha,
            backup_required:true,
            rollback_supported:true,
            minimum_php:"8.2.0",
            released_at:$released_at,
            product_identity:"VF Press / P06",
            notes:("V0.1.10 focuses the single-chapter Studio, enters private reading directly, repairs Reader typography and legacy demo line breaks, and keeps Schema 3. Formal release and remote asset readback bind exact source SHA " + $source_sha + ".")
          }' > projects/P06.json
          jq -e '.project_id=="P06" and .target_version=="0.1.10" and .update_type=="ATOMIC" and .from_versions==["0.1.9"] and .schema_from=="3" and .schema_to=="3" and .backup_required==true and .rollback_supported==true and .asset_bytes>0 and (.asset_sha256|test("^[a-f0-9]{64}$"))' projects/P06.json >/dev/null
          git config user.name 'VF Public Runner'
          git config user.email 'vf-public-runner@users.noreply.github.com'
          git add projects/P06.json
          git commit -m "release(P06): publish V0.1.10 update manifest"
          git pull --rebase origin main
          git push origin HEAD:main
          echo "MANIFEST_SHA=$(git rev-parse HEAD)" >> "$GITHUB_ENV"
          echo CORE_UPDATES_MANIFEST_PUBLISHED=PASS

      - name: Verify online discovery contract
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
        run: |
          set -Eeuo pipefail
          for attempt in $(seq 1 10); do
            gh api "repos/$CORE_REPO/contents/projects/P06.json?ref=main" --jq .content | tr -d '\n' | base64 -d >/tmp/P06-online.json
            if jq -e --arg sha "$UPDATE_SHA" --argjson bytes "$UPDATE_BYTES" '.target_version=="0.1.10" and .update_type=="ATOMIC" and .from_versions==["0.1.9"] and .schema_from=="3" and .schema_to=="3" and .release_tag=="v0.1.10" and .asset_bytes==$bytes and .asset_sha256==$sha' /tmp/P06-online.json >/dev/null; then
              break
            fi
            sleep 2
          done
          jq -e --arg sha "$UPDATE_SHA" --argjson bytes "$UPDATE_BYTES" '.target_version=="0.1.10" and .update_type=="ATOMIC" and .from_versions==["0.1.9"] and .schema_from=="3" and .schema_to=="3" and .release_tag=="v0.1.10" and .asset_bytes==$bytes and .asset_sha256==$sha' /tmp/P06-online.json >/dev/null
          cd source
          php -r 'require "vendor/autoload.php"; $m=json_decode(file_get_contents("/tmp/P06-online.json"),true,512,JSON_THROW_ON_ERROR); $r=(new CoreUpdates\UpdateCore("P06"))->check("0.1.9","3",$m); if(($r["status"]??"")!=="AVAILABLE"){fwrite(STDERR,json_encode($r));exit(1);} echo "ONLINE_DISCOVERY=AVAILABLE\n";'
          echo ONLINE_UPDATE_DISCOVERY=PASS
          echo FORMAL_RELEASE=PASS
          echo OWNER_RUNTIME_VISUAL_REVIEW=PENDING_AFTER_PRODUCTION_UPDATE
          echo PRODUCTION_WRITE=NO
