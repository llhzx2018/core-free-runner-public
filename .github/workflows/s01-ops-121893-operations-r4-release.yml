name: S01 Ops 1.21.893 Operations R4 Release Gate

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  candidate:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      TARGET_REPO: llhzx2018/vf-tools-ops
      CANDIDATE_SHA: 943f3d56300c196b919c585b14a1f47e9d8be9fd
      BASE_SHA: 2c76b6fe6e2b085b0265439b0c57b3da1ad2d67a
      VERSION: 1.21.893
      PREVIOUS_TAG: v1.21.892
      PREVIOUS_ASSET: vf-tools-ops_V1.21.892.zip
      ASSET: vf-tools-ops_V1.21.893.zip

    steps:
      - name: Checkout exact Ops main
        uses: actions/checkout@v4
        with:
          repository: llhzx2018/vf-tools-ops
          ref: 943f3d56300c196b919c585b14a1f47e9d8be9fd
          token: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
          fetch-depth: 0
          path: ops
          persist-credentials: false

      - name: Exact identity and scope fence
        shell: bash
        run: |
          set -Eeuo pipefail
          test "$(git -C ops rev-parse HEAD)" = "$CANDIDATE_SHA"
          test "$(tr -d '\r\n' < ops/VERSION)" = "$VERSION"
          grep -Fq 'Version: 1.21.893' ops/vf-ops.php
          grep -Fq "VF_OPS_VERSION', '1.21.893'" ops/vf-ops.php
          grep -Fq 'S01-CLOUDFLARE-OPERATIONS-R4' ops/vf-ops.php
          actual="$(git -C ops diff --name-only "$BASE_SHA" "$CANDIDATE_SHA" | sort)"
          expected="$(printf '%s\n' \
            CHANGELOG.md \
            VERSION \
            assets/css/current/admin-s01-cloudflare-operations-r4.css \
            assets/js/admin-s01-domain-shell-r1.js \
            includes/admin-v13/views/content_workflow_v6.php \
            includes/admin-v13/views/seo_today_v6.php \
            includes/views/admin-style-helpers/admin-s01-uiux-polish.php \
            includes/views/admin-style-helpers/admin-style-core.php \
            tests/s01-c02-121893-cloudflare-operations-r4-contract.php \
            vf-ops.php | sort)"
          test "$actual" = "$expected"
          test "$(git -C ops diff --name-only "$BASE_SHA" "$CANDIDATE_SHA" -- database config includes/v6 includes/wp-update | wc -l)" = "0"
          echo PASS_EXACT_IDENTITY_AND_SCOPE

      - name: Contract syntax navigation and UX gate
        shell: bash
        run: |
          set -Eeuo pipefail
          php ops/tests/s01-c02-121893-cloudflare-operations-r4-contract.php
          php -l ops/vf-ops.php >/dev/null
          php -l ops/includes/admin-v13/views/content_workflow_v6.php >/dev/null
          php -l ops/includes/admin-v13/views/seo_today_v6.php >/dev/null
          php -l ops/includes/views/admin-style-helpers/admin-s01-uiux-polish.php >/dev/null
          php -l ops/includes/views/admin-style-helpers/admin-style-core.php >/dev/null
          php -l ops/tests/s01-c02-121893-cloudflare-operations-r4-contract.php >/dev/null
          node --check ops/assets/js/admin-s01-domain-shell-r1.js >/dev/null
          css=ops/assets/css/current/admin-s01-cloudflare-operations-r4.css
          for needle in             '.vf-v9-ops-workspace'             '.vf-v9-table'             '.vf-v6-dashboard-page'             '.vf-v6-quality-page'             '.vf-v6-publish-overview'             '.vf-v6-system-overview'             '.vf-s01-tools-workbench'             '#toplevel_page_vf-toolsite-ops .wp-submenu li[hidden]'             '@media (max-width:782px)'             '@media (max-width:520px)'             '@media (forced-colors:active)'             '@media (prefers-reduced-motion:reduce)'; do
            grep -Fq "$needle" "$css"
          done
          ! grep -Fq 'position: fixed' "$css"
          ! grep -Fq 'position:fixed' "$css"
          python3 - <<'PY'
          from pathlib import Path
          core=Path('ops/includes/views/admin-style-helpers/admin-style-core.php').read_text()
          assert "'admin-s01-domain-shell-r1.js'" in core
          assert 'window.vfOpsS01AdminShell=' in core
          js=Path('ops/assets/js/admin-s01-domain-shell-r1.js').read_text()
          for x in ("li.classList.remove('current')",'li.hidden = true','li.hidden = false'):
              assert x in js
          content=Path('ops/includes/admin-v13/views/content_workflow_v6.php').read_text()
          seo=Path('ops/includes/admin-v13/views/seo_today_v6.php').read_text()
          assert 'data-vf-v9-workspace="content"' in content
          assert 'data-vf-v9-workspace="seo"' in seo
          assert 'SEO 工作区' in seo
          polish=Path('ops/includes/views/admin-style-helpers/admin-s01-uiux-polish.php').read_text()
          assert polish.index('admin-s01-cloudflare-operations-r4.css') > polish.index('admin-s01-cloudflare-content-seo-r2.css')
          PY
          echo PASS_CONTRACT_SYNTAX_NAVIGATION_UX

      - name: Security and ownership fence
        shell: bash
        run: |
          set -Eeuo pipefail
          ! grep -RInE 'gh[pousr]_[A-Za-z0-9_]{20,}|github_pat_[A-Za-z0-9_]{20,}|BEGIN (RSA|OPENSSH|EC) PRIVATE KEY' \
            ops/vf-ops.php \
            ops/assets/js/admin-s01-domain-shell-r1.js \
            ops/includes/admin-v13/views/content_workflow_v6.php \
            ops/includes/admin-v13/views/seo_today_v6.php \
            ops/includes/views/admin-style-helpers/admin-s01-uiux-polish.php \
            ops/includes/views/admin-style-helpers/admin-style-core.php \
            ops/assets/css/current/admin-s01-cloudflare-operations-r4.css
          test "$(git -C ops diff --name-only "$BASE_SHA" "$CANDIDATE_SHA" -- includes/wp-update config database | wc -l)" = "0"
          echo PASS_SECURITY_AND_OWNERSHIP_FENCE

      - name: Build deterministic runtime-only package
        shell: bash
        run: |
          set -Eeuo pipefail
          rm -rf stage dist
          mkdir -p stage/vf-ops dist
          python3 - <<'PY'
          from pathlib import Path
          import hashlib,re,zipfile
          root=Path('ops'); stage=Path('stage/vf-ops')
          def runtime(rel):
              rel=rel.replace('\\','/').lstrip('/')
              if not rel or '../' in rel:return False
              if re.search(r'(^|/)(?:\.git|\.github|tests?|docs?|evidence|private|tmp|temp|cache|logs?)(/|$)',rel,re.I):return False
              if re.search(r'\.(?:sql|sqlite|sqlite3|db|log|zip|tar|gz|bak|tmp)$',rel,re.I):return False
              if '/' not in rel:return rel in ('vf-ops.php','uninstall.php')
              return re.match(r'^(?:includes|assets|languages|config)/',rel) is not None
          files=[]
          for p in sorted((x for x in root.rglob('*') if x.is_file()),key=lambda x:x.relative_to(root).as_posix()):
              rel=p.relative_to(root).as_posix()
              if not runtime(rel):continue
              if p.is_symlink():raise SystemExit('runtime symlink forbidden: '+rel)
              data=p.read_bytes(); files.append((rel,data))
              q=stage/rel; q.parent.mkdir(parents=True,exist_ok=True); q.write_bytes(data)
          canonical=b''.join(rel.encode()+b'\0'+hashlib.sha256(data).hexdigest().encode()+b'\n' for rel,data in files)
          Path('/tmp/runtime_count').write_text(str(len(files)))
          Path('/tmp/runtime_fp').write_text(hashlib.sha256(canonical).hexdigest())
          for out in ('dist/vf-tools-ops_V1.21.893.zip','dist/rebuild-vf-tools-ops_V1.21.893.zip'):
              with zipfile.ZipFile(out,'w',compression=zipfile.ZIP_DEFLATED,compresslevel=9) as z:
                  for rel,data in files:
                      info=zipfile.ZipInfo('vf-ops/'+rel,date_time=(2020,1,1,0,0,0))
                      info.compress_type=zipfile.ZIP_DEFLATED
                      info.external_attr=(0o100644 & 0xFFFF)<<16
                      z.writestr(info,data)
          print('RUNTIME_FILES='+str(len(files)))
          print('RUNTIME_FINGERPRINT='+hashlib.sha256(canonical).hexdigest())
          PY
          cmp "dist/$ASSET" "dist/rebuild-$ASSET"
          unzip -tq "dist/$ASSET" >/dev/null
          unzip -p "dist/$ASSET" vf-ops/vf-ops.php | grep -Fq 'Version: 1.21.893'
          unzip -Z1 "dist/$ASSET" | grep -Fxq 'vf-ops/assets/css/current/admin-s01-cloudflare-operations-r4.css'
          test "$(cat /tmp/runtime_count)" = "964"
          sha256sum "dist/$ASSET" | awk '{print $1}' >/tmp/asset_sha
          stat -c '%s' "dist/$ASSET" >/tmp/asset_bytes
          echo PASS_DETERMINISTIC_RUNTIME_PACKAGE

      - name: Previous release compatibility
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
        run: |
          set -Eeuo pipefail
          mkdir -p previous
          gh release download "$PREVIOUS_TAG" --repo "$TARGET_REPO" --pattern "$PREVIOUS_ASSET" --dir previous
          test "$(stat -c '%s' "previous/$PREVIOUS_ASSET")" = "3440242"
          test "$(sha256sum "previous/$PREVIOUS_ASSET" | awk '{print $1}')" = "3a5503133bff2f0c871775c5c09f62da5eb953c0330c2576f1638e1c9336c4a2"
          unzip -tq "previous/$PREVIOUS_ASSET" >/dev/null
          unzip -Z1 "previous/$PREVIOUS_ASSET" | grep -v '/$' | sort >/tmp/prev_files.txt
          unzip -Z1 "dist/$ASSET" | grep -v '/$' | sort >/tmp/new_files.txt
          comm -13 /tmp/prev_files.txt /tmp/new_files.txt >/tmp/added.txt
          comm -23 /tmp/prev_files.txt /tmp/new_files.txt >/tmp/removed.txt
          test "$(cat /tmp/added.txt)" = 'vf-ops/assets/css/current/admin-s01-cloudflare-operations-r4.css'
          test ! -s /tmp/removed.txt
          echo PASS_PREVIOUS_RELEASE_COMPATIBILITY

      - name: Candidate receipt
        shell: bash
        run: |
          set -Eeuo pipefail
          cat > dist/candidate-receipt.txt <<EOF
          COMPONENT=S01-C02
          VERSION=$VERSION
          SOURCE_SHA=$CANDIDATE_SHA
          BASE_RELEASE=$PREVIOUS_TAG
          ASSET=$ASSET
          BYTES=$(cat /tmp/asset_bytes)
          SHA256=$(cat /tmp/asset_sha)
          RUNTIME_FILES=$(cat /tmp/runtime_count)
          RUNTIME_FINGERPRINT=$(cat /tmp/runtime_fp)
          RUNTIME_ADDED=assets/css/current/admin-s01-cloudflare-operations-r4.css
          RUNTIME_REMOVED=NONE
          SECURITY=PASS
          TESTING=PASS
          NAVIGATION=PASS_SIX_PRIMARY_DOMAINS
          UA_UI=PASS_CLOUDFLARE_OPERATIONS_R4_CONTRACT
          SCHEMA=6.0.0_TO_6.0.0
          DATA_MIGRATION=NONE
          SIX_DOMAIN_IA=UNCHANGED
          CANONICAL_ROUTES=UNCHANGED
          M3U8_OWNERSHIP=UNCHANGED
          THEME_OWNERSHIP=UNCHANGED
          SHARED_UPDATER=UNCHANGED
          PRODUCTION=NOT_RUN
          OWNER_VISUAL_ACCEPTANCE=NOT_RUN
          RESULT=PASS
          EOF
          cat dist/candidate-receipt.txt

      - name: Upload exact tested candidate
        uses: actions/upload-artifact@v4
        with:
          name: s01-c02-121893-operations-r4-candidate
          path: |
            dist/vf-tools-ops_V1.21.893.zip
            dist/candidate-receipt.txt
          if-no-files-found: error
          retention-days: 14


  formal-release:
    needs: candidate
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      TARGET_REPO: llhzx2018/vf-tools-ops
      SOURCE_SHA: 943f3d56300c196b919c585b14a1f47e9d8be9fd
      VERSION: 1.21.893
      TAG: v1.21.893
      ASSET: vf-tools-ops_V1.21.893.zip

    steps:
      - name: Download exact tested candidate
        uses: actions/download-artifact@v4
        with:
          name: s01-c02-121893-operations-r4-candidate
          path: tested

      - name: Verify tested artifact identity
        shell: bash
        run: |
          set -Eeuo pipefail
          test -f "tested/$ASSET"
          test -f tested/candidate-receipt.txt
          grep -Fqx "VERSION=$VERSION" tested/candidate-receipt.txt
          grep -Fqx "SOURCE_SHA=$SOURCE_SHA" tested/candidate-receipt.txt
          grep -Fqx "ASSET=$ASSET" tested/candidate-receipt.txt
          grep -Fqx 'RESULT=PASS' tested/candidate-receipt.txt
          unzip -tq "tested/$ASSET" >/dev/null
          unzip -p "tested/$ASSET" vf-ops/vf-ops.php | grep -Fq 'Version: 1.21.893'
          echo PASS_EXACT_TESTED_ARTIFACT

      - name: Create exact formal release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.VF_RELEASE_WRITE_TOKEN }}
        run: |
          set -Eeuo pipefail
          test -n "$GH_TOKEN"
          if gh release view "$TAG" --repo "$TARGET_REPO" >/dev/null 2>&1; then
            echo "Release already exists; refusing overwrite." >&2
            exit 31
          fi
          test "$(gh api "repos/$TARGET_REPO/commits/$SOURCE_SHA" --jq '.sha')" = "$SOURCE_SHA"
          BYTES="$(stat -c '%s' "tested/$ASSET")"
          SHA="$(sha256sum "tested/$ASSET" | awk '{print $1}')"
          RUNTIME_FILES="$(awk -F= '$1=="RUNTIME_FILES"{print $2}' tested/candidate-receipt.txt)"
          RUNTIME_FP="$(awk -F= '$1=="RUNTIME_FINGERPRINT"{print $2}' tested/candidate-receipt.txt)"
          cat > /tmp/release-notes.md <<EOF
          # VF Tools Ops V1.21.893

          **S01-C02 · Cloudflare-class Operations Workspaces R4**

          This release completes the six-domain mature operations layout:
          - one compact VF 站点运营 / Ops product shell
          - six primary domains only in the visible sidebar
          - Content and SEO rebuilt around object + status + issue + action
          - Dashboard, Quality, Publish and System flattened into operational rows
          - Tools Golden Workspace retained and aligned
          - child routes remain registered server-side while projection is presentation-only

          Safety:
          - Six-domain IA unchanged
          - Canonical routes unchanged
          - Schema 6.0.0 -> 6.0.0
          - Data Migration NONE
          - M3U8 ownership unchanged
          - Theme ownership unchanged
          - Shared updater unchanged
          - Rollback v1.21.892
          - OWNER visual acceptance requires post-update readback

          Exact asset:
          - $ASSET
          - Bytes $BYTES
          - SHA-256 $SHA
          - Runtime files $RUNTIME_FILES
          - Runtime Fingerprint $RUNTIME_FP
          EOF
          created=0
          cleanup(){ rc=$?; if [ "$rc" -ne 0 ] && [ "$created" -eq 1 ]; then gh release delete "$TAG" --repo "$TARGET_REPO" --cleanup-tag -y >/dev/null 2>&1 || true; fi; exit "$rc"; }
          trap cleanup EXIT
          gh release create "$TAG" "tested/$ASSET#VF Tools Ops $VERSION" \
            --repo "$TARGET_REPO" \
            --target "$SOURCE_SHA" \
            --title "VF Tools Ops V$VERSION" \
            --notes-file /tmp/release-notes.md
          created=1
          trap - EXIT
          echo PASS_FORMAL_RELEASE_CREATED

      - name: Independent remote release readback
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.VF_RELEASE_WRITE_TOKEN }}
        run: |
          set -Eeuo pipefail
          JSON="$(gh api "repos/$TARGET_REPO/releases/tags/$TAG")"
          RID="$(jq -r '.id' <<<"$JSON")"
          AID="$(jq -r --arg n "$ASSET" '.assets[]|select(.name==$n)|.id' <<<"$JSON")"
          REMOTE_BYTES="$(jq -r --arg n "$ASSET" '.assets[]|select(.name==$n)|.size' <<<"$JSON")"
          test -n "$RID" && test "$RID" != null
          test -n "$AID" && test "$AID" != null
          test "$REMOTE_BYTES" = "$(stat -c '%s' "tested/$ASSET")"
          test "$(gh api "repos/$TARGET_REPO/git/ref/tags/$TAG" --jq '.object.sha')" = "$SOURCE_SHA"
          mkdir -p remote
          gh release download "$TAG" --repo "$TARGET_REPO" --pattern "$ASSET" --dir remote
          test "$(sha256sum "remote/$ASSET" | awk '{print $1}')" = "$(sha256sum "tested/$ASSET" | awk '{print $1}')"
          cmp "tested/$ASSET" "remote/$ASSET"
          cat > formal-release-receipt.txt <<EOF
          COMPONENT=S01-C02
          VERSION=$VERSION
          SOURCE_SHA=$SOURCE_SHA
          TAG=$TAG
          RELEASE_ID=$RID
          ASSET=$ASSET
          ASSET_ID=$AID
          BYTES=$REMOTE_BYTES
          SHA256=$(sha256sum "remote/$ASSET" | awk '{print $1}')
          RUNTIME_FILES=$(awk -F= '$1=="RUNTIME_FILES"{print $2}' tested/candidate-receipt.txt)
          RUNTIME_FINGERPRINT=$(awk -F= '$1=="RUNTIME_FINGERPRINT"{print $2}' tested/candidate-receipt.txt)
          TESTED_ARTIFACT_EQUALS_DELIVERED_ARTIFACT=PASS
          REMOTE_READBACK=PASS
          PRODUCTION=NOT_YET_CLAIMED
          OWNER_VISUAL_ACCEPTANCE=OPEN
          RESULT=PASS
          EOF
          cat formal-release-receipt.txt

      - name: Upload formal release receipt
        uses: actions/upload-artifact@v4
        with:
          name: s01-c02-121893-operations-r4-formal-release
          path: formal-release-receipt.txt
          if-no-files-found: error
          retention-days: 30
