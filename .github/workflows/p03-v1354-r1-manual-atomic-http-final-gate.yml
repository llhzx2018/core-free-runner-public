name: P03 V1.35.4 R1 Manual Atomic HTTP Final Gate

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/p03-v1354-r1-manual-atomic-http-final-gate.yml'
      - 'scripts/p03_v1354_r1_manual_atomic_http_final_gate.sh'

permissions:
  contents: read
  actions: read

concurrency:
  group: p03-v1354-r1-manual-atomic-http-final-gate
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 70
    env:
      DIST_BUILD_COMMIT: 1c9993ae94566a6b83c8bd53e71b01761aa37f59
      DIST_BUILD_TREE: 7a216956ef6fb81aee2de08b55c4275af91323a9
      FROZEN_PRODUCT_COMMIT: af34b84a3135333cf05077b3eb64e22ef6b3afef
      FROZEN_PRODUCT_TREE: 08eee7e8c891a57c357553dd5de20c1a7bd79849
      PRODUCTION_COMMIT: 263c4d8f7e38bff58db5680a6845ceb5a576d6ae
      UPDATE_ASSET_ID: '517498815'
      UPDATE_BYTES: '766304'
      UPDATE_SHA256: dcc784332669ae4c2dc5ec9bc723cdc174c631b6ea2dc8c9d87ae28135a52141
      ORIGINAL_UPDATE_ASSET_ID: '517455108'
      ORIGINAL_UPDATE_SHA256: c063d99e8ab99b86da345599c8db2f27d961b3565df94daafd203e867d8c8073
      FIXTURE_PASS: VfP03-Manual-R1-2026!
      PHP_TEST_IMAGE: vf-forge-v1354-manual-r1-php84
      GATE_ROOT: /tmp/p03-v1354-manual-r1

    steps:
      - name: Checkout public harness
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          repository: llhzx2018/core-free-runner-public
          ref: main
          fetch-depth: 1
          persist-credentials: false
          path: harness

      - name: Require private read credential
        shell: bash
        env:
          READ_TOKEN: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
        run: |
          set -Eeuo pipefail
          test -n "$READ_TOKEN"
          echo 'PRIVATE_READ_CREDENTIAL=PASS'

      - name: Checkout exact Distribution Build Commit
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          repository: llhzx2018/vf-forge
          ref: ${{ env.DIST_BUILD_COMMIT }}
          token: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
          fetch-depth: 1
          persist-credentials: false
          path: distribution

      - name: Checkout exact Frozen Product
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          repository: llhzx2018/vf-forge
          ref: ${{ env.FROZEN_PRODUCT_COMMIT }}
          token: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
          fetch-depth: 1
          persist-credentials: false
          path: product

      - name: Checkout exact Production V1.35.3
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          repository: llhzx2018/vf-forge
          ref: ${{ env.PRODUCTION_COMMIT }}
          token: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
          fetch-depth: 1
          persist-credentials: false
          path: production

      - name: Prepare PHP 8.4 SQLite Zip cURL runtime
        shell: bash
        run: |
          set -Eeuo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq php-cli php-sqlite3 php-curl php-zip sqlite3 curl unzip >/dev/null
          cat >"$RUNNER_TEMP/Dockerfile.p03-manual-r1" <<'EOF'
          FROM php:8.4-cli-bookworm@sha256:5380a7dd17b978cd14fb2c84cd67ddc47adaf079b240f8138224bbadefae46f7
          ARG HOST_UID=1001
          ARG HOST_GID=121
          RUN apt-get update && apt-get install -y --no-install-recommends libzip-dev libsqlite3-dev libcurl4-openssl-dev sqlite3 && docker-php-ext-install zip pdo_sqlite curl && rm -rf /var/lib/apt/lists/*
          USER ${HOST_UID}:${HOST_GID}
          EOF
          docker build --build-arg "HOST_UID=$(id -u)" --build-arg "HOST_GID=$(id -g)" -t "$PHP_TEST_IMAGE" -f "$RUNNER_TEMP/Dockerfile.p03-manual-r1" "$RUNNER_TEMP" >/dev/null
          docker run --rm "$PHP_TEST_IMAGE" php -r 'foreach(["pdo_sqlite","sqlite3","zip","curl"] as $x){if(!extension_loaded($x)){exit(1);}} echo PHP_VERSION," MANUAL_R1_RUNTIME_PASS\n";'

      - name: Execute real HTTP Manual Atomic Bridge final gate
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.VF_PRIVATE_READ_TOKEN }}
        run: |
          set -Eeuo pipefail
          chmod +x harness/scripts/p03_v1354_r1_manual_atomic_http_final_gate.sh
          bash -n harness/scripts/p03_v1354_r1_manual_atomic_http_final_gate.sh
          bash harness/scripts/p03_v1354_r1_manual_atomic_http_final_gate.sh

      - name: Verify zero public artifacts and cleanup
        if: always()
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -Eeuo pipefail
          docker rm -f p03-dist-r1-upgrade p03-dist-r1-recovery p03-v1354-upgrade p03-v1354-full-fresh >/dev/null 2>&1 || true
          rm -rf distribution product production /tmp/p03-v1354-* "$RUNNER_TEMP/Dockerfile.p03-manual-r1" "$RUNNER_TEMP/p03_v1354_r1_manual_http_patched_gate.sh"
          RESP=$(curl -fsS -H 'Accept: application/vnd.github+json' -H "Authorization: Bearer $GH_TOKEN" -H 'X-GitHub-Api-Version: 2022-11-28' "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}/artifacts")
          test "$(python3 -c 'import json,sys;print(json.load(sys.stdin)["total_count"])' <<<"$RESP")" = 0
          echo 'PUBLIC_ACTIONS_ARTIFACTS=0'
          echo 'EPHEMERAL_CLEANUP=PASS'

      - name: Final verdict
        shell: bash
        run: |
          echo 'P03_V1354_R1_MANUAL_ATOMIC_BRIDGE_FINAL_GATE=PASS'
          echo 'CORE_UPDATES=QUARANTINED_ENABLED_FALSE'
          echo 'LEGACY_AUTOMATIC_DISCOVERY=UNSUPPORTED_EXACT_CONTRACT'
          echo 'PRODUCTION_VERSION=1.35.3'
          echo 'PRODUCTION_SCHEMA=29'
          echo 'PRODUCTION_WRITE=0'
          echo 'V1.35.5=NOT_CREATED'
          echo 'NEXT=STOP_FOR_MASTER_MANUAL_PRODUCTION_UPGRADE_AUTHORIZATION'
