# 核心代理运行时 V1.1 A/B 验证候选说明

> 状态：`TEMPORARY CANDIDATE / CLOSE WITHOUT MERGE BY DEFAULT`  
> Public Runner：`llhzx2018/core-free-runner-public`  
> Baseline：`core-agent@03cddccd1ac713fe408cd797bd8d7473903fa550`  
> Candidate：`core-agent@f4edbec876edc06a5bd422edb496e9ff24186282`

## 目的

使用公共 GitHub Hosted Runner 临时读取两个精确的私有源码 Commit，验证 V1.0 当前稳定基线与 V1.1 Candidate。私有源码只存在于 Runner 临时工作区，不进入公共 Git、Artifact 或 Evidence。

## 验证范围

- 两个版本各自执行完整 `scripts/verify.py`；
- 比较机器测试数量没有减少；
- 验证 L0–L3 预算完全匹配治理合同；
- 验证 L0 零 Skill、L1 只加载 `skill-dev`、L3 Release 隔离；
- 验证 80% Checkpoint、预算超限升级阻断、Stop Rule；
- 验证 Project Context Snapshot 不超过 4 KiB；
- 只上传脱敏的状态、数量、SHA、布尔检查结果。

## 不做的事情

- 不读取或使用 `VF_RELEASE_WRITE_TOKEN`；
- 不写入 `core-agent`；
- 不修改 `main`、Tag、Release 或 Production；
- 不上传私有源码、原始测试日志、Secret 或真实数据；
- 不把控制面约束测试冒充成真实模型 Token 测量。

本 Harness 只能证明 Runtime 预算和 Lazy Load 约束已被机器执行。真实 Token 节省比例仍需后续小规模模型 Canary 才能测量。
