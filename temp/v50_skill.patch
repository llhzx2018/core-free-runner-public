diff --git a/skills/skill-book/V5.0/SKILL.md b/skills/skill-book/V5.0/SKILL.md
index 62bc7fa..cfd6857 100644
--- a/skills/skill-book/V5.0/SKILL.md
+++ b/skills/skill-book/V5.0/SKILL.md
@@ -3,7 +3,7 @@ name: skill-book
 description: 开发图书、电子书、教程、课程与知识产品。以“读得下去、学得会、练得到、做得出”为最高读者结果，覆盖读者定义、学习架构、研究来源、版权隐私、正文写作、阅读吸引力、理解迁移、练习训练、实操验证、完整旧稿基线、逐章深度回归、生成期深度控制、资产等价与出版交付。仅用于明确的书籍、教程、课程或知识产品任务；软件需求、代码开发、技术审计和软件Release不得调用。
 ---
 
-# skill-book V4.9 Candidate
+# skill-book V5.0 Candidate
 
 本Skill独立于软件开发链。软件项目、电子书资产与`skill-book`方法身份不得混成同一对象、版本或Release。
 
@@ -39,7 +39,7 @@ DO — 能把关键路径实际做出来，并得到可验证输出
 书不以厚度、字数、文件数或模板数证明价值；短也不自动优于长。
 
 
-## 0A. Runtime Hard Entry Protocol · V4.9 可证明执行入口
+## 0A. Runtime Hard Entry Protocol · V5.0 可证明执行入口
 
 `FULL_BOOK` / `SEALED_STANDALONE_FORWARD`开始后，第一份运行资产必须先证明当前过程读取了本 Skill 内部合同，而不是只根据外部 Prompt 模拟。**Draft 前**先写：
 
@@ -47,7 +47,7 @@ DO — 能把关键路径实际做出来，并得到可验证输出
 evidence/runtime_entry_receipt.json
 ```
 
-Receipt 必须包含 Skill 内部固定 `RUNTIME_CONTRACT_ID = SB49-RUNTIME-CONTRACT-4D71B2E9`、`declared_skill_version = 4.9`、`entry_stage = PRE_DRAFT` 和本轮 mandatory evidence list。正式 Benchmark Prompt 不得携带这个 ID。详细合同见 `references/runtime_entry_receipt_contract.md`。
+Receipt 必须包含 Skill 内部固定 `RUNTIME_CONTRACT_ID = SB50-RUNTIME-CONTRACT-73C1A9E4`、`declared_skill_version = 5.0`、`entry_stage = PRE_DRAFT` 和本轮 mandatory evidence list。正式 Benchmark Prompt 不得携带这个 ID。详细合同见 `references/runtime_entry_receipt_contract.md`。
 
 随后以下不是建议，而是**必须产生的运行资产**。如果任一适用资产不存在，不允许在 Manifest 或最终审计中写 `ACCEPT / PASS / READY / REPLACE`：
 
@@ -55,10 +55,16 @@ Receipt 必须包含 Skill 内部固定 `RUNTIME_CONTRACT_ID = SB49-RUNTIME-CONT
 evidence/reader_transformation_contract.json
 evidence/generation_responsibility_contract.json
 evidence/generation_responsibility_pre_draft.json
+evidence/operational_closure_contract.json
+evidence/operational_closure_pre_draft.json
+evidence/training_feedback_contract.json
 evidence/adequacy_contract.json
 evidence/adequacy_audit.json
 evidence/prefreeze_random_open.json
-evidence/prefreeze_random_open_holdout.json
+evidence/shadow_local_value.json
+evidence/strict_unseen_holdout_receipt.json
+evidence/canonical_evidence_state.json
+evidence/canonical_evidence_requirements.json
 evidence/runtime_acceptance_audit.json
 evidence/random_open_holdout.json
 ```
@@ -76,14 +82,14 @@ POST_DRAFT_BASELINE_DIFFERENTIAL = NOT_RUN
 FINAL_DECISION = BLOCK
 ```
 
-### V4.9 Runtime Truth Rules
+### V5.0 Runtime Truth Rules
 
 1. **Runtime Entry Receipt is mandatory**：缺少或 ID / Skill version 不匹配时，`RUNTIME_ENTRY_RECEIPT = BLOCK`。Receipt 是 Skill-internal canary，不是密码学证明；最终仍由 External Verifier复核。
 2. **Declarative Evidence First / External Verifier Authority**：Personal Skill 对话不保证包内 Python 一定以独立 Tool 暴露。若本轮无法真实执行 bundled verifier，必须把相应 `machine_execution_status = NOT_RUN` 并让最终 Decision 保持 `BLOCK`；不得另写一套更弱的 `audit/run_audit.py`、prose checklist 或启发式检查替代正式机器 Authority。
 3. **Prose audit is not machine evidence**：`ADEQUACY_AUDIT.md`、总结文字、表格或“全部COVERED”不能替代`adequacy_contract.json + adequacy_audit.json`。
 4. **Workbook cannot rescue a thin template**：Workbook可以是操作视图，但每个HIGH复杂度Practical Asset必须有自足的Reader-facing Template/Worksheet定义；不能用一个XLSX表头替代判断规则、失败恢复、Quality Bar与完成条件。
 5. **Worked Example cannot rescue the blank tool**：完整案例只能证明示例存在，不能抵消空白模板只有几行字段。
-6. **Random Open repair must use a holdout**：第一次Random Open发现失败后可以修稿，但不得只用同一组样本证明修复成功；必须用未参与修复的holdout seed再抽样。只对已抽中段落补字属于`RANDOM_OPEN_OVERFIT`。
+6. **Generator-visible holdout is not strict holdout**：生成器只允许执行GENERATION Random Open；Freeze前另跑Shadow Local-Value。真正严格Holdout由外部Evaluator产生opaque receipt，生成器不得看到seed/path/window。任何具体holdout样本已暴露给当前模型上下文时必须标`STRICT_UNSEEN_MODEL_HOLDOUT = NOT_PROVEN`。
 7. **Real Reader Evidence has a strict meaning**：社区讨论、市场研究、公开帖子、专家意见都不是“真人读过这本书”的证据。没有目标读者实际阅读/学习/训练这份Candidate时，`REAL_READER_EVIDENCE = NOT_RUN`，不得写`PARTIAL`。
 8. **Final Decision is downstream of gates**：任何适用硬门`BLOCK / FAIL / NOT_RUN / UNKNOWN`，最终决策不得出现`ACCEPT / PASS / REPLACE / PROMOTE`。
 9. **Canonical reader-path directories are preferred**：正文放`chapters/`或`manuscript/`；实操资产放`templates/`；Evidence放`evidence/`。如交付层另有`book/chapters`或`assets/`镜像，机器审计必须仍能发现，不得因目录变化漏检。
@@ -105,22 +111,34 @@ V4.9保留V4.8全部Runtime Truth Enforcement，不撤销Entry Receipt、Externa
 
 详细合同见`references/generation_responsibility_contract.md`与`references/prefreeze_random_open_contract.md`。这些Gate是Canonical-independent：SEALED模式下Freeze前仍禁止读取旧稿；旧Canonical只在冻结后的Evaluator阶段进入Post-Draft Differential。
 
+V5.0继续保留V4.9的Generation Responsibility与V4.8的Runtime Truth Enforcement，但根据V4.9 B1 fresh Forward Benchmark新增六个系统性修复：
 
-### V4.9 Packaging Truth Rule
+1. `Operational Closure Matrix`：防止Reader Contract一开始就漏掉measurement、diagnosis、operating cadence、repeatable workflow等高级职责；
+2. `Training Feedback Coverage`：HIGH training chapter必须有Expected Output → Reference Judgment → Error → Correction → Retry → Completion；
+3. `Canonical Evidence Promotion`：repair rerun的PASS必须原子晋升到唯一canonical evidence path，hash/run_id/timestamp不一致即BLOCK；
+4. `Shadow Local-Value + Strict Opaque Holdout`：Freeze前先用shadow gate降低规则漂移，真正holdout由外部Evaluator提供不暴露seed/path/window的receipt；
+5. `Mapping-aware Practical Asset Fidelity`：旧资产改名后按Preservation Ledger exact old→new path与职责维度比较，不再只靠same-name ratio；
+6. `Workbook / Version Discovery Robustness`：支持root `WORKBOOK.md`、`workbook/`与Manifest声明路径，并要求所有V5.0 executable audit output使用V5.0版本标识。
+
+详细合同：`references/operational_closure_contract.md`、`references/training_feedback_contract.md`、`references/canonical_evidence_promotion_contract.md`、`references/shadow_local_value_contract.md`、`references/strict_holdout_contract.md`。
+
+
+### V5.0 Packaging Truth Rule
 
 Packaging 前必须检查：
 
 ```text
-runtime_entry_receipt.json exists + contract id matches
+runtime_entry_receipt.json exists + V5.0 contract id matches
 → reader_transformation_contract.json exists
-→ generation_responsibility_contract.json exists
-→ generation_responsibility_pre_draft.json = PASS
-→ generation_responsibility_pre_freeze.json = PASS
-→ adequacy_contract.json exists
-→ formal adequacy result exists or explicitly NOT_RUN/BLOCK
-→ prefreeze_random_open.json = PASS
-→ prefreeze_random_open_holdout.json = PASS
-→ independent post-freeze random-open holdout exists
+→ generation_responsibility PRE_DRAFT/PRE_FREEZE canonical evidence = PASS
+→ operational_closure PRE_DRAFT/PRE_FREEZE canonical evidence = PASS
+→ training_feedback PRE_FREEZE canonical evidence = PASS
+→ adequacy canonical evidence = PASS
+→ generation Random Open canonical evidence = PASS
+→ shadow_local_value canonical evidence = PASS
+→ strict_unseen_holdout_receipt = PASS and opaque
+→ canonical_evidence_promotion_audit = PASS
+→ independent post-freeze runtime holdout exists
 → SEALED mode has post-draft baseline result, or explicitly BLOCK_EXTERNAL_BASELINE_REQUIRED
 → runtime_acceptance final result
 → only then package Manifest
@@ -159,6 +177,11 @@ BOOK.OP.PUBLISH_DELIVERY
 - FULL_BOOK生成期深度：`references/generation_depth_contract.md`
 - 复杂度/资产充分度：`references/adequacy_contract.md`
 - Runtime Entry Receipt：`references/runtime_entry_receipt_contract.md`
+- Operational Closure：`references/operational_closure_contract.md`
+- Training Feedback：`references/training_feedback_contract.md`
+- Canonical Evidence Promotion：`references/canonical_evidence_promotion_contract.md`
+- Shadow Local-Value：`references/shadow_local_value_contract.md`
+- Strict Unseen Holdout：`references/strict_holdout_contract.md`
 - 正式盲测与封存后基线审计：`references/sealed_standalone_forward_contract.md`
 - 跨窗口恢复：`references/vf_agent_runtime_contract.md`
 
@@ -235,6 +258,8 @@ V4.8 额外要求：`adequacy_contract.json` 与 Chapter Depth Plan 必须在对
 
 V4.9再要求：Depth Plan不得只证明“计划中的章节够深”。Draft前必须先运行`generation_responsibility_audit.py --stage PRE_DRAFT`，把Reader Contract中的关键JUDGE/DO能力和实战型Lifecycle责任映射到计划章节与Practical Assets；首次Freeze前再以`--stage PRE_FREEZE`对真实路径复核。任一BLOCK不得Freeze。
 
+V5.0再要求：Reader Contract之后、Depth Plan之前必须建立Operational Closure Matrix；HIGH training chapter在Freeze前必须通过Training Feedback Coverage。Generation Responsibility只证明“已声明职责没有丢”，Operational Closure负责阻止“职责从一开始就没声明”。两者不可互相替代。
+
 普通STANDALONE若允许使用Canonical结构预算，Depth Plan可登记旧Reading Core和Chapter Value Bundle的非表达性预算。`SEALED_STANDALONE_FORWARD`禁止登记任何旧版预算；它改用V4.6 Adequacy Contract从第一性原理校准复杂度，冻结后再做Baseline Differential。
 
 ## 7. Chapter Value Bundle与Progressive Depth
@@ -273,7 +298,7 @@ Random Open不再只在Final Gate执行。每生成一个Reading Core、Referenc
 
 失败资产必须在进入下一批前修复。不得等全书完成后再一次性发现系统性薄弱。
 
-V4.9把这一要求升级为机器Evidence：首次Freeze前必须真实执行`prefreeze_random_open_audit.py`的GENERATION与HOLDOUT两组seed，并分别落盘`evidence/prefreeze_random_open.json`与`evidence/prefreeze_random_open_holdout.json`。第一次失败允许回修，但Holdout失败不得Freeze。Freeze后的Runtime Acceptance仍使用另一组External holdout，不能用生成期自检替代。
+V5.0中`prefreeze_random_open_audit.py`只保留GENERATION接口，固定可见的HOLDOUT接口被移除。Freeze前还必须通过`shadow_local_value_audit.py`；真正strict holdout只能由外部Evaluator产生opaque `strict_unseen_holdout_receipt.json`。Generator只能看到aggregate/failure class，不得看到seed/path/window；修复后的PASS必须通过Canonical Evidence Promotion晋升到正式evidence path。
 
 ## 9. Reference Differentiation · 新增
 
