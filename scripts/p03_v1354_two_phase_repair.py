from __future__ import annotations
import json


def repair_php(payload_b64:str,payload_sha:str,manifest_sha:str,m030_sha:str,migration_fp:str,test_fail_stage:str='')->bytes:
    fail=json.dumps(test_fail_stage,separators=(',',':'))
    template=r'''<?php
/** VF Forge 1.35.4 Formal Atomic Maintenance Release. Frozen Product Candidate; two-request Schema30 executor. */
declare(strict_types=1);
const VFF_PACKAGE_ID='vf-forge';
const VFF_PACKAGE_TYPE='app';
const VFF_SOURCE_VERSION='1.35.3';
const VFF_SOURCE_SCHEMA=29;
const VFF_ATOMIC_TARGET='1.35.4';
const VFF_ATOMIC_SCHEMA=30;
const VFF_MIGRATION_ID='M030_EXTERNAL_AUTHORITY_MEMORY_INDEX';
const VFF_MIGRATION_RUNTIME_PATH='app/schema30.sql';
const VFF_MIGRATION_RUNTIME_SHA256='__M030_SHA__';
const VFF_MIGRATION_CHECKSUM='__MIGRATION_FP__';
const VFF_PRODUCT_COMMIT='af34b84a3135333cf05077b3eb64e22ef6b3afef';
const VFF_PRODUCT_TREE='08eee7e8c891a57c357553dd5de20c1a7bd79849';
const VFF_RUNTIME_FINGERPRINT='2fd3ebbbebfd7155371fe44664715cbe34f63cfb98dfeb691bba90d4864ca083';
const VFF_ATOMIC_PAYLOAD_JSON_SHA256='__PAYLOAD_SHA__';
const VFF_ATOMIC_SOURCE_MANIFEST_SHA256='__MANIFEST_SHA__';
const VFF_ATOMIC_PAYLOAD='__PAYLOAD_B64__';
const VFF_TEST_FAIL_STAGE=__FAIL__;

function vff_headers():void{header('Content-Type: text/html; charset=utf-8');header('X-Robots-Tag: noindex, nofollow, noarchive');header('X-Frame-Options: DENY');header('X-Content-Type-Options: nosniff');header('Referrer-Policy: no-referrer');header('Cache-Control: no-store');}
function vff_render(string $title,string $message,string $type='normal',bool $form=false,string $csrf=''):never{$t=htmlspecialchars($title,ENT_QUOTES,'UTF-8');$m=htmlspecialchars($message,ENT_QUOTES,'UTF-8');$c=$type==='error'?'#b62c25':'#12a46b';vff_headers();echo '<!doctype html><html lang="zh-CN"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>VF Forge 原子升级</title><body style="margin:0;padding:36px 18px;font:15px/1.65 Segoe UI,Microsoft YaHei,sans-serif;background:#f7fbf9;color:#173029"><main style="width:min(760px,100%);margin:auto;background:#fff;border:1px solid #dfe9e4;border-radius:16px;padding:28px"><div>P03 · VF Forge · Formal Atomic</div><h1>'.$t.'</h1><div style="padding:13px;border-left:4px solid '.$c.'">'.$m.'</div>';if($form)echo '<form method="post"><input type="hidden" name="_csrf" value="'.htmlspecialchars($csrf,ENT_QUOTES,'UTF-8').'"><input type="hidden" name="confirmation" value="UPGRADE"><button style="margin-top:18px;padding:10px 17px" type="submit">执行原子升级</button></form>';echo '</main></body></html>';exit;}
function vff_phase_handoff(string $csrf,string $op,string $nonce):never{vff_headers();echo '<!doctype html><html lang="zh-CN"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>VF Forge Schema 30 迁移</title><body><main><h1>源码切换完成</h1><p>正在进入独立 PHP Request 执行 Schema 29 → 30 / M030。</p><form id="m" method="post"><input type="hidden" name="_csrf" value="'.htmlspecialchars($csrf,ENT_QUOTES,'UTF-8').'"><input type="hidden" name="phase" value="migrate"><input type="hidden" name="op" value="'.htmlspecialchars($op,ENT_QUOTES,'UTF-8').'"><input type="hidden" name="nonce" value="'.htmlspecialchars($nonce,ENT_QUOTES,'UTF-8').'"><input type="hidden" name="confirmation" value="MIGRATE"><button type="submit">继续 Schema 30 迁移</button></form><script>document.getElementById("m").submit();</script></main></body></html>';exit;}
function vff_safe_rel(string $rel):string{$rel=str_replace('\\','/',$rel);if($rel===''||str_starts_with($rel,'/')||str_contains($rel,"\0")||preg_match('#(^|/)\.\.(/|$)#',$rel)||preg_match('/^[A-Za-z]:/',$rel))throw new RuntimeException('Unsafe Atomic path: '.$rel);$top=explode('/',$rel,2)[0];$roots=['api.php','diagnose.php','download.php','index.html','index.php','maintenance.php','memory-api.php','robots.txt','setup.php','share.php'];if(!in_array($rel,$roots,true)&&!in_array($top,['app','assets','cli','mcp'],true))throw new RuntimeException('Atomic path outside managed runtime scope: '.$rel);if($rel==='app/.runtime.php'||preg_match('#(^|/)repair-v[^/]+\.php$#',$rel))throw new RuntimeException('Atomic payload contains private repair path.');return$rel;}
function vff_mkdir(string $d,int $mode=0750):void{if(is_link($d))throw new RuntimeException('Symlink directory rejected.');if(!is_dir($d)&&!mkdir($d,$mode,true)&&!is_dir($d))throw new RuntimeException('Cannot create directory.');}
function vff_rrmdir(string $p):void{if(!is_dir($p)||is_link($p))return;$it=new RecursiveIteratorIterator(new RecursiveDirectoryIterator($p,FilesystemIterator::SKIP_DOTS),RecursiveIteratorIterator::CHILD_FIRST);foreach($it as $x){$q=$x->getPathname();if($x->isLink()||$x->isFile())@unlink($q);else@rmdir($q);}@rmdir($p);}
function vff_atomic_write(string $p,string $b):void{vff_mkdir(dirname($p));if(is_link($p))throw new RuntimeException('Symlink target rejected.');$t=$p.'.vff-new-'.bin2hex(random_bytes(5));if(file_put_contents($t,$b,LOCK_EX)!==strlen($b)){@unlink($t);throw new RuntimeException('Atomic write failed.');}@chmod($t,0644);if(!@rename($t,$p)){@unlink($t);throw new RuntimeException('Atomic rename failed.');}}
function vff_copy_verified(string $a,string $b):void{vff_mkdir(dirname($b));if(!copy($a,$b))throw new RuntimeException('Backup copy failed.');if(!hash_equals((string)hash_file('sha256',$a),(string)hash_file('sha256',$b)))throw new RuntimeException('Backup SHA mismatch.');}
function vff_db_check(PDO $db):void{if(strtolower((string)$db->query('PRAGMA integrity_check')->fetchColumn())!=='ok')throw new RuntimeException('SQLite integrity_check failed.');if($db->query('PRAGMA foreign_key_check')->fetchAll())throw new RuntimeException('SQLite foreign_key_check failed.');}
function vff_failpoint(string $s):void{if(VFF_TEST_FAIL_STAGE!==''&&hash_equals(VFF_TEST_FAIL_STAGE,$s))throw new RuntimeException('TEST_FAILPOINT_'.$s);}
function vff_payload():array{$gz=base64_decode(VFF_ATOMIC_PAYLOAD,true);if(!is_string($gz))throw new RuntimeException('Atomic payload base64 invalid.');$raw=gzdecode($gz);if(!is_string($raw)||!hash_equals(VFF_ATOMIC_PAYLOAD_JSON_SHA256,hash('sha256',$raw)))throw new RuntimeException('Atomic payload checksum invalid.');$p=json_decode($raw,true,64,JSON_THROW_ON_ERROR);if(($p['format']??'')!=='vf-forge-atomic-payload-v2'||($p['package_id']??'')!==VFF_PACKAGE_ID||($p['source_version']??'')!==VFF_SOURCE_VERSION||($p['target_version']??'')!==VFF_ATOMIC_TARGET||(int)($p['source_schema']??0)!==VFF_SOURCE_SCHEMA||(int)($p['target_schema']??0)!==VFF_ATOMIC_SCHEMA||($p['migration']??'')!==VFF_MIGRATION_ID)throw new RuntimeException('Atomic Package Identity mismatch.');if(!hash_equals((string)($p['source_manifest_sha256']??''),VFF_ATOMIC_SOURCE_MANIFEST_SHA256))throw new RuntimeException('Atomic source manifest mismatch.');return$p;}
function vff_parse_bootstrap(string $f):array{$s=@file_get_contents($f);if(!is_string($s))throw new RuntimeException('bootstrap unavailable.');if(!preg_match("/define\\('VFAB_VERSION',\\s*'([^']+)'\\);/",$s,$v)||!preg_match("/define\\('VFAB_SCHEMA_VERSION',\\s*(\\d+)\\);/",$s,$m))throw new RuntimeException('bootstrap identity unreadable.');return[$v[1],(int)$m[1]];}
function vff_https():bool{if(!empty($_SERVER['HTTPS'])&&strtolower((string)$_SERVER['HTTPS'])!=='off')return true;if((int)($_SERVER['SERVER_PORT']??0)===443)return true;$trust=strtolower(trim((string)(getenv('VF_FORGE_TRUST_PROXY_HTTPS')?:'')));if(in_array($trust,['1','true','yes','on'],true))return strtolower(trim(explode(',',(string)($_SERVER['HTTP_X_FORWARDED_PROTO']??''),2)[0]))==='https';return false;}
function vff_same_origin():bool{$o=trim((string)($_SERVER['HTTP_ORIGIN']??''));if($o==='')return true;$h=parse_url($o,PHP_URL_HOST);$s=strtolower((string)(parse_url($o,PHP_URL_SCHEME)??''));$p=parse_url($o,PHP_URL_PORT);$rh=parse_url('http://'.(string)($_SERVER['HTTP_HOST']??''),PHP_URL_HOST);$rp=parse_url('http://'.(string)($_SERVER['HTTP_HOST']??''),PHP_URL_PORT);if(!is_string($h)||!is_string($rh)||!hash_equals(strtolower(rtrim($h,'.')),strtolower(rtrim($rh,'.'))))return false;$rs=vff_https()?'https':'http';if(!hash_equals($rs,$s))return false;return($p??($s==='https'?443:80))===($rp??((int)($_SERVER['SERVER_PORT']??0)?:($rs==='https'?443:80)));}
function vff_auth(string $root):array{$rf=$root.'/app/.runtime.php';if(!is_file($rf)||is_link($rf))throw new RuntimeException('Runtime settings unavailable.');$rt=include $rf;if(!is_array($rt)||empty($rt['data_root'])||empty($rt['db_file'])||empty($rt['config_file']))throw new RuntimeException('Runtime settings invalid.');$raw=@file_get_contents((string)$rt['config_file']);$cfg=is_string($raw)?json_decode($raw,true):null;if(!is_array($cfg))throw new RuntimeException('Config unavailable.');if(session_status()!==PHP_SESSION_ACTIVE){session_name('vfforge_session');ini_set('session.use_strict_mode','1');ini_set('session.use_only_cookies','1');session_start();}if(empty($_SESSION['vfab_admin']))vff_render('需要登录','请先登录 VF Forge 管理后台。','error');$epoch=hash('sha256',(string)($cfg['admin_password_hash']??'').'|'.(string)($cfg['session_secret']??'').'|'.(string)($cfg['password_updated_at']??''));$stored=(string)($_SESSION['vfab_auth_epoch']??'');if($stored===''||!hash_equals($epoch,$stored))vff_render('登录已失效','认证纪元不一致，请重新登录。','error');$sid=session_id();$secret=(string)($cfg['session_secret']??$cfg['admin_password_hash']??'vf-forge');$sh=$sid===''?'':hash_hmac('sha256',$sid,$secret);$db=new PDO('sqlite:'.(string)$rt['db_file'],null,null,[PDO::ATTR_ERRMODE=>PDO::ERRMODE_EXCEPTION,PDO::ATTR_DEFAULT_FETCH_MODE=>PDO::FETCH_ASSOC]);$db->exec('PRAGMA foreign_keys=ON');$q=$db->prepare('SELECT revoked_at,expires_at FROM admin_sessions WHERE session_hash=? LIMIT 1');$q->execute([$sh]);$row=$q->fetch();if(!$row||!empty($row['revoked_at'])||((strtotime((string)($row['expires_at']??''))?:PHP_INT_MAX)<=time()))vff_render('登录已失效','当前管理员 Session 已失效。','error');return[$rt,$cfg,$db,(string)($_SESSION['vfab_csrf']??'')];}
function vff_meta_write(string $path,array $m):void{$raw=json_encode($m,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES);if(!is_string($raw))throw new RuntimeException('Recovery metadata encode failed.');vff_atomic_write($path,$raw);@chmod($path,0640);}
function vff_context(string $root,array $rt,string $op,string $nonce):array{if(!preg_match('/^\d{8}-\d{6}-[a-f0-9]{8}$/',$op)||!preg_match('/^[a-f0-9]{64}$/',$nonce))throw new RuntimeException('Atomic continuation token invalid.');$recovery=(string)$rt['data_root'].'/backups/atomic-recovery-v'.VFF_SOURCE_VERSION.'-to-v'.VFF_ATOMIC_TARGET.'-'.$op;$metaPath=$recovery.'/RECOVERY.json';if(!is_file($metaPath)||is_link($metaPath))throw new RuntimeException('Atomic recovery metadata missing.');$m=json_decode((string)file_get_contents($metaPath),true);if(!is_array($m)||($m['format']??'')!=='vf-forge-atomic-recovery-v2'||($m['from_version']??'')!==VFF_SOURCE_VERSION||($m['target_version']??'')!==VFF_ATOMIC_TARGET||(int)($m['schema_from']??0)!==VFF_SOURCE_SCHEMA||(int)($m['schema_to']??0)!==VFF_ATOMIC_SCHEMA||($m['migration']??'')!==VFF_MIGRATION_ID||!hash_equals((string)($m['nonce_sha256']??''),hash('sha256',$nonce))||!hash_equals((string)($m['source_manifest_sha256']??''),VFF_ATOMIC_SOURCE_MANIFEST_SHA256))throw new RuntimeException('Atomic recovery identity mismatch.');return['recovery'=>$recovery,'meta_path'=>$metaPath,'meta'=>$m,'source_backup'=>$recovery.'/source','db_snapshot'=>$recovery.'/pre-upgrade.sqlite','db_file'=>(string)$rt['db_file'],'lock_path'=>(string)$rt['data_root'].'/locks/atomic-upgrade.lock'];}
function vff_restore(string $root,array $ctx):void{$m=$ctx['meta'];$db=(string)$ctx['db_file'];$snap=(string)$ctx['db_snapshot'];@unlink($db.'-wal');@unlink($db.'-shm');if(is_file($snap)&&!is_link($snap)){$b=file_get_contents($snap);if(is_string($b))vff_atomic_write($db,$b);}foreach(array_reverse(array_values(array_unique((array)($m['changed_paths']??[])))) as $rel){try{$rel=vff_safe_rel((string)$rel);$bak=$ctx['source_backup'].'/'.$rel;$target=$root.'/'.$rel;if(is_file($bak)&&!is_link($bak)){$b=file_get_contents($bak);if(is_string($b))vff_atomic_write($target,$b);}elseif(in_array($rel,(array)($m['absent_before']??[]),true)&&is_file($target)&&!is_link($target))@unlink($target);}catch(Throwable $ignore){}}clearstatcache(true);if(function_exists('opcache_reset'))@opcache_reset();$m['status']='RECOVERY_APPLIED';$m['recovered_at']=gmdate('c');try{vff_meta_write($ctx['meta_path'],$m);}catch(Throwable $ignore){}}

$root=__DIR__;$phase=(string)($_POST['phase']??'');[$current,$currentSchema]=vff_parse_bootstrap($root.'/app/bootstrap.php');[$rt,$cfg,$authDb,$csrf]=vff_auth($root);

if($phase==='migrate'){
 if($_SERVER['REQUEST_METHOD']!=='POST'||!vff_same_origin())vff_render('来源校验失败','Schema 迁移续接只接受同源 POST。','error');$provided=(string)($_POST['_csrf']??'');if($provided===''||$csrf===''||!hash_equals($csrf,$provided)||((string)($_POST['confirmation']??''))!=='MIGRATE')vff_render('续接校验失败','Schema 迁移续接认证无效。','error');
 if($current!==VFF_ATOMIC_TARGET||$currentSchema!==VFF_ATOMIC_SCHEMA)vff_render('目标源码身份错误','Schema 迁移续接要求已切换到 V'.VFF_ATOMIC_TARGET.' / Code Schema '.VFF_ATOMIC_SCHEMA.'。','error');$op=(string)($_POST['op']??'');$nonce=(string)($_POST['nonce']??'');$ctx=null;$lock=null;$migrateDb=null;
 try{$ctx=vff_context($root,$rt,$op,$nonce);if(($ctx['meta']['status']??'')!=='SOURCE_SWITCHED_PENDING_MIGRATION')throw new RuntimeException('Atomic continuation state invalid.');vff_mkdir(dirname($ctx['lock_path']));$lock=fopen($ctx['lock_path'],'c+');if(!$lock||!flock($lock,LOCK_EX|LOCK_NB))throw new RuntimeException('另一个 Atomic/维护任务正在执行。');
  $payload=vff_payload();$files=(array)$payload['files'];foreach($files as $rel=>$spec){$t=$root.'/'.vff_safe_rel((string)$rel);if(!is_file($t)||is_link($t)||!hash_equals((string)$spec['sha256'],(string)hash_file('sha256',$t)))throw new RuntimeException('Pre-migration target runtime verification failed: '.$rel);}if(!hash_equals(VFF_MIGRATION_RUNTIME_SHA256,(string)hash_file('sha256',$root.'/'.VFF_MIGRATION_RUNTIME_PATH)))throw new RuntimeException('Migration 030 runtime checksum mismatch.');vff_failpoint('before_migration');
  $authDb=null;require_once $root.'/app/bootstrap.php';require_once $root.'/app/MigrationRunner.php';$migrateDb=vfab_open_sqlite_file((string)$ctx['db_file'],false);$mr=(new VfMigrationRunner($migrateDb))->run();if((int)($mr['before_schema']??-1)!==VFF_SOURCE_SCHEMA||(int)($mr['after_schema']??-1)!==VFF_ATOMIC_SCHEMA||($mr['migration_id']??'')!==VFF_MIGRATION_ID||empty($mr['applied'])||empty($mr['verified'])||!hash_equals(VFF_MIGRATION_CHECKSUM,(string)($mr['checksum']??'')))throw new RuntimeException('Migration 030 result contract mismatch.');vff_db_check($migrateDb);$q=$migrateDb->prepare('SELECT migration_id,checksum,status FROM schema_migrations WHERE version=?');$q->execute([VFF_ATOMIC_SCHEMA]);$row=$q->fetch();if(!$row||($row['migration_id']??'')!==VFF_MIGRATION_ID||($row['status']??'')!=='applied'||!hash_equals(VFF_MIGRATION_CHECKSUM,(string)($row['checksum']??'')))throw new RuntimeException('Migration 030 identity/checksum readback mismatch.');if((int)$migrateDb->query('SELECT MAX(version) FROM schema_migrations')->fetchColumn()!==VFF_ATOMIC_SCHEMA)throw new RuntimeException('Schema 30 max-version readback mismatch.');vff_failpoint('after_migration');
  foreach($files as $rel=>$spec){$t=$root.'/'.vff_safe_rel((string)$rel);if(!is_file($t)||is_link($t)||!hash_equals((string)$spec['sha256'],(string)hash_file('sha256',$t)))throw new RuntimeException('Final runtime verification failed: '.$rel);}vff_failpoint('before_success');$m=$ctx['meta'];$m['status']='COMPLETE';$m['completed_at']=gmdate('c');vff_meta_write($ctx['meta_path'],$m);$migrateDb=null;if(is_resource($lock)){flock($lock,LOCK_UN);fclose($lock);$lock=null;}@unlink(__FILE__);vff_render('升级完成','VF Forge 已原子升级到 V'.VFF_ATOMIC_TARGET.'；Schema '.VFF_SOURCE_SCHEMA.' → '.VFF_ATOMIC_SCHEMA.' / '.VFF_MIGRATION_ID.' PASS。升级前源码与 SQLite 恢复点已保留。');
 }catch(Throwable $e){$migrateDb=null;$authDb=null;if(is_array($ctx))vff_restore($root,$ctx);if(is_resource($lock)){@flock($lock,LOCK_UN);@fclose($lock);}vff_render('原子升级失败，已执行恢复',$e->getMessage().'；源码与 SQLite 已按升级前恢复点恢复，恢复快照继续保留。','error');}
}

if($current!==VFF_SOURCE_VERSION||$currentSchema!==VFF_SOURCE_SCHEMA)vff_render('版本不允许','仅允许 V'.VFF_SOURCE_VERSION.' / Schema '.VFF_SOURCE_SCHEMA.' → V'.VFF_ATOMIC_TARGET.' / Schema '.VFF_ATOMIC_SCHEMA.'。','error');
if($_SERVER['REQUEST_METHOD']!=='POST')vff_render('VF Forge V'.VFF_SOURCE_VERSION.' → V'.VFF_ATOMIC_TARGET,'Schema '.VFF_SOURCE_SCHEMA.' → '.VFF_ATOMIC_SCHEMA.'；Migration '.VFF_MIGRATION_ID.'。执行前建立源码与 SQLite 恢复点，失败自动恢复。','normal',true,$csrf);
if(!vff_same_origin())vff_render('来源校验失败','请求 Origin 与当前站点不一致。','error');$provided=(string)($_POST['_csrf']??'');if($provided===''||$csrf===''||!hash_equals($csrf,$provided))vff_render('CSRF 校验失败','登录状态已变化，请刷新后重试。','error');if((string)($_POST['confirmation']??'')!=='UPGRADE')vff_render('确认失败','原子升级确认参数无效。','error');
$payload=vff_payload();$files=(array)($payload['files']??[]);$delete=(array)($payload['delete_paths']??[]);if(!$files)throw new RuntimeException('Atomic payload empty.');if(!isset($files[VFF_MIGRATION_RUNTIME_PATH])||!hash_equals(VFF_MIGRATION_RUNTIME_SHA256,(string)($files[VFF_MIGRATION_RUNTIME_PATH]['sha256']??'')))throw new RuntimeException('Migration 030 missing from Atomic payload.');
$dataRoot=(string)$rt['data_root'];$dbFile=(string)$rt['db_file'];$tempDir=$dataRoot.'/temp';$backupDir=$dataRoot.'/backups';$lockDir=$dataRoot.'/locks';$op=gmdate('Ymd-His').'-'.bin2hex(random_bytes(4));$nonce=bin2hex(random_bytes(32));$stage=$tempDir.'/atomic-'.$op;$recovery=$backupDir.'/atomic-recovery-v'.VFF_SOURCE_VERSION.'-to-v'.VFF_ATOMIC_TARGET.'-'.$op;$sourceBackup=$recovery.'/source';$dbSnapshot=$recovery.'/pre-upgrade.sqlite';$meta=$recovery.'/RECOVERY.json';$lockPath=$lockDir.'/atomic-upgrade.lock';vff_mkdir($stage);vff_mkdir($sourceBackup);vff_mkdir($lockDir);$lock=fopen($lockPath,'c+');if(!$lock||!flock($lock,LOCK_EX|LOCK_NB))throw new RuntimeException('另一个 Atomic/维护任务正在执行。');$changed=[];$absent=[];$ctx=null;
try{foreach($files as $rel=>$spec){$rel=vff_safe_rel((string)$rel);$b=base64_decode((string)($spec['content']??''),true);if(!is_string($b)||(int)($spec['bytes']??-1)!==strlen($b)||!hash_equals((string)$spec['sha256'],hash('sha256',$b)))throw new RuntimeException('Payload file verify failed: '.$rel);$d=$stage.'/'.$rel;vff_mkdir(dirname($d));if(file_put_contents($d,$b,LOCK_EX)!==strlen($b))throw new RuntimeException('Stage write failed: '.$rel);}vff_failpoint('after_stage');
 vff_db_check($authDb);try{$authDb->exec('PRAGMA wal_checkpoint(TRUNCATE)');}catch(Throwable $ignore){}$authDb->exec('VACUUM INTO '.$authDb->quote($dbSnapshot));if(!is_file($dbSnapshot)||filesize($dbSnapshot)<=0)throw new RuntimeException('Pre-upgrade SQLite snapshot failed.');$verify=new PDO('sqlite:'.$dbSnapshot);vff_db_check($verify);$verify=null;
 foreach($files as $rel=>$spec){$rel=vff_safe_rel((string)$rel);$t=$root.'/'.$rel;$wanted=(string)$spec['sha256'];if(is_file($t)&&!is_link($t)&&hash_equals($wanted,(string)hash_file('sha256',$t)))continue;if(file_exists($t)||is_link($t)){if(!is_file($t)||is_link($t))throw new RuntimeException('Unsafe source path: '.$rel);vff_copy_verified($t,$sourceBackup.'/'.$rel);}else{$absent[]=$rel;}$changed[]=$rel;}foreach($delete as $rel){$rel=vff_safe_rel((string)$rel);$t=$root.'/'.$rel;if(file_exists($t)||is_link($t)){if(!is_file($t)||is_link($t))throw new RuntimeException('Unsafe delete path: '.$rel);vff_copy_verified($t,$sourceBackup.'/'.$rel);$changed[]=$rel;}}
 $metaData=['format'=>'vf-forge-atomic-recovery-v2','from_version'=>VFF_SOURCE_VERSION,'target_version'=>VFF_ATOMIC_TARGET,'schema_from'=>VFF_SOURCE_SCHEMA,'schema_to'=>VFF_ATOMIC_SCHEMA,'migration'=>VFF_MIGRATION_ID,'status'=>'PREPARED','changed_paths'=>array_values(array_unique($changed)),'absent_before'=>$absent,'db_snapshot'=>'pre-upgrade.sqlite','source_manifest_sha256'=>VFF_ATOMIC_SOURCE_MANIFEST_SHA256,'nonce_sha256'=>hash('sha256',$nonce),'created_at'=>gmdate('c')];vff_meta_write($meta,$metaData);$ctx=['recovery'=>$recovery,'meta_path'=>$meta,'meta'=>$metaData,'source_backup'=>$sourceBackup,'db_snapshot'=>$dbSnapshot,'db_file'=>$dbFile,'lock_path'=>$lockPath];vff_failpoint('after_recovery');
 $order=array_keys($files);usort($order,static function($a,$b){$aa=$a==='app/bootstrap.php'?1:0;$bb=$b==='app/bootstrap.php'?1:0;return$aa<=>$bb?:strcmp($a,$b);});foreach($order as $rel){$rel=vff_safe_rel((string)$rel);$t=$root.'/'.$rel;$wanted=(string)$files[$rel]['sha256'];if(is_file($t)&&!is_link($t)&&hash_equals($wanted,(string)hash_file('sha256',$t)))continue;$b=file_get_contents($stage.'/'.$rel);if(!is_string($b))throw new RuntimeException('Staged source missing: '.$rel);vff_atomic_write($t,$b);if(!hash_equals($wanted,(string)hash_file('sha256',$t)))throw new RuntimeException('Post-switch SHA mismatch: '.$rel);}foreach($delete as $rel){$t=$root.'/'.vff_safe_rel((string)$rel);if(is_file($t)&&!is_link($t)&&!@unlink($t))throw new RuntimeException('Obsolete runtime removal failed: '.$rel);}clearstatcache(true);if(function_exists('opcache_reset'))@opcache_reset();[$nv,$ns]=vff_parse_bootstrap($root.'/app/bootstrap.php');if($nv!==VFF_ATOMIC_TARGET||$ns!==VFF_ATOMIC_SCHEMA)throw new RuntimeException('Target bootstrap identity mismatch.');foreach($files as $rel=>$spec){$t=$root.'/'.vff_safe_rel((string)$rel);if(!is_file($t)||is_link($t)||!hash_equals((string)$spec['sha256'],(string)hash_file('sha256',$t)))throw new RuntimeException('Post-switch target runtime verification failed: '.$rel);}vff_failpoint('after_source_switch');$metaData['status']='SOURCE_SWITCHED_PENDING_MIGRATION';$metaData['source_switched_at']=gmdate('c');vff_meta_write($meta,$metaData);vff_rrmdir($stage);if(is_resource($lock)){flock($lock,LOCK_UN);fclose($lock);$lock=null;}vff_phase_handoff($csrf,$op,$nonce);
}catch(Throwable $e){$authDb=null;if(is_array($ctx))vff_restore($root,$ctx);if(is_resource($lock)){@flock($lock,LOCK_UN);@fclose($lock);}vff_rrmdir($stage);vff_render('原子升级失败，已执行恢复',$e->getMessage().'；源码与 SQLite 已按升级前恢复点恢复，恢复快照继续保留。','error');}
'''
    return (template.replace('__M030_SHA__',m030_sha).replace('__MIGRATION_FP__',migration_fp).replace('__PAYLOAD_SHA__',payload_sha).replace('__MANIFEST_SHA__',manifest_sha).replace('__PAYLOAD_B64__',payload_b64).replace('__FAIL__',fail)).encode()
